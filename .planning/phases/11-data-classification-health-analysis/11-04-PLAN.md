---
phase: 11-data-classification-health-analysis
plan: 04
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - backend/migrations/035_version_health_checks.sql
  - backend/pkg/models/version_health_models.go
  - backend/internal/storage/version_health_store.go
  - backend/internal/api/handlers_version_health.go
  - backend/internal/api/server.go
autonomous: true
requirements:
  - VER-03
must_haves:
  truths:
    - "User can view version-specific health checks for PostgreSQL 11-17"
    - "Health checks adapt queries based on PostgreSQL version"
    - "System shows different checks for EOL versions (11-12) vs active versions (13-17)"
    - "Health check results include severity and remediation suggestions"
  artifacts:
    - path: "backend/migrations/035_version_health_checks.sql"
      provides: "Database schema and seed data for version-specific health checks"
      min_lines: 80
    - path: "backend/pkg/models/version_health_models.go"
      provides: "Data structures for health checks"
      exports: ["VersionHealthCheck", "HealthCheckResult", "VersionHealthCheckResponse"]
    - path: "backend/internal/storage/version_health_store.go"
      provides: "Storage for version-specific health checks"
      exports: ["GetHealthChecksForVersion", "StoreHealthCheckResult"]
    - path: "backend/internal/api/handlers_version_health.go"
      provides: "HTTP handlers for health check endpoints"
      exports: ["handleGetVersionHealthChecks", "handleRunHealthChecks"]
  key_links:
    - from: "backend/internal/api/handlers_version_health.go"
      to: "backend/internal/storage/version_health_store.go"
      via: "storage.PostgresDB methods"
      pattern: "p\.store\."
    - from: "backend/internal/storage/version_health_store.go"
      to: "backend/pkg/models/version_health_models.go"
      via: "VersionHealthCheck struct"
      pattern: "VersionHealthCheck"
---

<objective>
Implement version-specific health checks for PostgreSQL 11-17 with adaptive queries, severity levels, and remediation suggestions. Enable users to understand version-specific health concerns and get actionable recommendations.

Purpose: Support the full range of PostgreSQL versions (including EOL 11-12 for migration scenarios) with health checks that adapt to version-specific features, deprecated settings, and known issues.
Output: Complete version-specific health check system with seeded checks and REST API.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-data-classification-health-analysis/11-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase for executor reference -->

From backend/pkg/models/version_models.go:
```go
type VersionCapabilities struct {
    Version        int    `json:"version" db:"version"`
    VersionString  string `json:"version_string" db:"version_string"`
    // ... version-specific capabilities
}
```

From backend/internal/storage/version_store.go:
```go
func (p *PostgresDB) GetVersionCapabilities(ctx context.Context, collectorID uuid.UUID) (*models.VersionCapabilities, error)
```

From backend/migrations/032_data_classification_tables.sql:
```go
-- Pattern: TimescaleDB hypertable creation
SELECT create_hypertable('metrics_host_health_scores', 'time', if_not_exists => TRUE);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create version health check models</name>
  <files>backend/pkg/models/version_health_models.go</files>
  <read_first>
    - backend/pkg/models/version_models.go (existing version models)
    - backend/pkg/models/health_models.go (health score models)
  </read_first>
  <behavior>
    - Test: VersionHealthCheck struct has min_version, max_version, check_name, check_query fields
    - Test: HealthCheckResult struct has result, passed, message fields
    - Test: VersionHealthCheckResponse includes collector version info
  </behavior>
  <action>Create version_health_models.go with the following structs:

1. VersionHealthCheck struct:
   - ID (int, db:"id", json:"id")
   - MinVersion (int, db:"min_version", json:"min_version") - Minimum PG version (e.g., 11)
   - MaxVersion (int, db:"max_version", json:"max_version") - Maximum PG version (NULL = no upper limit)
   - CheckName (string, db:"check_name", json:"check_name")
   - CheckQuery (string, db:"check_query", json:"check_query")
   - ExpectedResult (string, db:"expected_result", json:"expected_result")
   - Severity (string, db:"severity", json:"severity") - critical, warning, info
   - Description (string, db:"description", json:"description")
   - Remediation (string, db:"remediation", json:"remediation") - How to fix
   - Category (string, db:"category", json:"category") - performance, security, configuration

2. HealthCheckResult struct:
   - CheckID (int, db:"check_id", json:"check_id")
   - CheckName (string, db:"check_name", json:"check_name")
   - Severity (string, db:"severity", json:"severity")
   - Passed (bool, db:"passed", json:"passed")
   - ActualResult (string, db:"actual_result", json:"actual_result")
   - ExpectedResult (string, db:"expected_result", json:"expected_result")
   - Message (string, db:"message", json:"message")
   - Remediation (string, db:"remediation", json:"remediation")
   - CheckedAt (time.Time, db:"checked_at", json:"checked_at")

3. VersionHealthCheckResponse struct:
   - CollectorID (uuid.UUID, json:"collector_id")
   - PostgreSQLVersion (int, json:"postgresql_version")
   - PostgreSQLVersionString (string, json:"postgresql_version_string")
   - Results ([]*HealthCheckResult, json:"results")
   - Summary (HealthCheckSummary, json:"summary")

4. HealthCheckSummary struct:
   - TotalChecks (int, json:"total_checks")
   - PassedChecks (int, json:"passed_checks")
   - FailedCritical (int, json:"failed_critical")
   - FailedWarning (int, json:"failed_warning")
   - FailedInfo (int, json:"failed_info")

Use exact struct tags pattern from version_models.go.</action>
  <verify>
    <automated>go build ./backend/pkg/models</automated>
  </verify>
  <done>
    - version_health_models.go exists with all 4 structs
    - All structs have db and json tags
    - Package compiles successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Create version health checks migration with seed data</name>
  <files>backend/migrations/035_version_health_checks.sql</files>
  <read_first>
    - backend/migrations/032_data_classification_tables.sql (existing migration pattern)
    - backend/pkg/models/version_health_models.go (models from Task 1)
    - .planning/phases/11-data-classification-health-analysis/11-RESEARCH.md (health check examples)
  </read_first>
  <behavior>
    - Test: postgres_health_checks table exists with all columns
    - Test: Seed data includes checks for PG 11, 12, 13, 14, 15, 16, 17
    - Test: Checks include deprecated settings for EOL versions
  </behavior>
  <action>Create migration 035_version_health_checks.sql with:

1. postgres_health_checks table:
   - id SERIAL PRIMARY KEY
   - min_version INT NOT NULL
   - max_version INT (NULL = no upper limit)
   - check_name VARCHAR(255) NOT NULL
   - check_query TEXT NOT NULL
   - expected_result TEXT
   - severity VARCHAR(20) NOT NULL - critical, warning, info
   - description TEXT
   - remediation TEXT
   - category VARCHAR(50) DEFAULT 'configuration'
   - created_at TIMESTAMPTZ DEFAULT NOW()
   - updated_at TIMESTAMPTZ DEFAULT NOW()
   - UNIQUE(check_name, min_version)

2. Seed version-specific health checks:

-- PostgreSQL 11-12 (EOL versions - migration warnings)
INSERT INTO postgres_health_checks (min_version, max_version, check_name, check_query, expected_result, severity, description, remediation, category) VALUES
(11, 12, 'version_eol_warning',
 'SELECT current_setting(''server_version_num'')::int',
 'Version is End of Life',
 'critical', 'PostgreSQL 11-12 are End of Life versions',
 'Upgrade to PostgreSQL 15 or 16 for continued security updates',
 'security'),
(11, 12, 'wal_keep_segments_deprecated',
 'SELECT setting FROM pg_settings WHERE name = ''wal_keep_segments''',
 'Setting exists but deprecated in PG13+',
 'warning', 'wal_keep_segments is deprecated in PostgreSQL 13+',
 'Migrate to wal_keep_size before upgrading to PG13+',
 'configuration'),
(11, 12, 'pg_stat_replication_sync_state',
 'SELECT count(*) FROM pg_stat_replication WHERE sync_state = ''sync''',
 'Synchronous replicas count',
 'info', 'Check synchronous replication setup',
 'Ensure appropriate number of synchronous standbys',
 'replication');

-- PostgreSQL 13+ (Active versions)
INSERT INTO postgres_health_checks (min_version, max_version, check_name, check_query, expected_result, severity, description, remediation, category) VALUES
(13, NULL, 'wal_keep_size',
 'SELECT setting FROM pg_settings WHERE name = ''wal_keep_size''',
 'WAL retention size in MB',
 'warning', 'Check WAL retention size for replication',
 'Ensure sufficient WAL retention for standbys',
 'configuration'),
(13, NULL, 'wal_compression',
 'SELECT setting FROM pg_settings WHERE name = ''wal_compression''',
 'on or lz4 or zstd',
 'info', 'WAL compression can reduce I/O',
 'Enable wal_compression for better performance',
 'performance');

-- PostgreSQL 14+
INSERT INTO postgres_health_checks (min_version, max_version, check_name, check_query, expected_result, severity, description, remediation, category) VALUES
(14, NULL, 'pg_stat_wal_available',
 'SELECT count(*) FROM pg_stat_wal',
 'WAL statistics available',
 'info', 'PostgreSQL 14+ provides pg_stat_wal view',
 'Monitor WAL generation and I/O patterns',
 'monitoring'),
(14, NULL, 'logical_replication_workers',
 'SELECT count(*) FROM pg_stat_activity WHERE backend_type = ''logical replication worker''',
 'Logical replication workers count',
 'info', 'Check parallel apply workers in PG14+',
 'Monitor logical replication worker usage',
 'replication');

-- PostgreSQL 15+
INSERT INTO postgres_health_checks (min_version, max_version, check_name, check_query, expected_result, severity, description, remediation, category) VALUES
(15, NULL, 'logical_decoding_work_mem',
 'SELECT setting FROM pg_settings WHERE name = ''logical_decoding_work_mem''',
 'Memory for logical decoding',
 'info', 'PG15 allows tuning logical decoding memory',
 'Adjust for large transactions in logical replication',
 'configuration');

-- PostgreSQL 17+
INSERT INTO postgres_health_checks (min_version, max_version, check_name, check_query, expected_result, severity, description, remediation, category) VALUES
(17, NULL, 'parallel_apply_workers',
 'SELECT count(*) FROM pg_stat_activity WHERE backend_type LIKE ''parallel apply%''',
 'Parallel apply workers count',
 'info', 'PG17 supports parallel logical replication apply',
 'Monitor parallel apply for performance gains',
 'replication');

3. Create indexes:
   - CREATE INDEX idx_health_checks_version ON postgres_health_checks(min_version, max_version)
   - CREATE INDEX idx_health_checks_category ON postgres_health_checks(category)

Use BEGIN/COMMIT transaction wrapping.</action>
  <verify>
    <automated>grep -c "postgres_health_checks" backend/migrations/035_version_health_checks.sql && grep -c "INSERT INTO postgres_health_checks" backend/migrations/035_version_health_checks.sql</automated>
  </verify>
  <done>
    - Migration file exists with postgres_health_checks table
    - Seed data includes checks for PG 11-17
    - EOL version warnings included
    - Category and version indexes created
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create version health check store</name>
  <files>backend/internal/storage/version_health_store.go</files>
  <read_first>
    - backend/internal/storage/version_store.go (existing version detection)
    - backend/internal/storage/host_store.go (pattern for store methods)
    - backend/pkg/models/version_health_models.go (models from Task 1)
  </read_first>
  <behavior>
    - Test: GetHealthChecksForVersion returns correct checks for PG 14
    - Test: GetHealthChecksForVersion excludes PG 17 checks for PG 14
    - Test: RunHealthCheck executes query and returns result
  </behavior>
  <action>Create version_health_store.go with the following methods on PostgresDB:

1. GetHealthChecksForVersion(ctx context.Context, pgVersion int) ([]*models.VersionHealthCheck, error)
   - Query: SELECT * FROM postgres_health_checks WHERE min_version <= $1 AND (max_version IS NULL OR max_version >= $1) ORDER BY severity DESC, category
   - Return all applicable health checks for the given PostgreSQL version
   - Example: PG 14 returns checks with min_version <= 14 AND (max_version IS NULL OR max_version >= 14)

2. GetHealthCheckByID(ctx context.Context, id int) (*models.VersionHealthCheck, error)
   - Query: SELECT * FROM postgres_health_checks WHERE id = $1
   - Return single health check definition

3. RunHealthCheck(ctx context.Context, collectorID uuid.UUID, check *models.VersionHealthCheck) (*models.HealthCheckResult, error)
   - This is the core execution logic:
     1. Get connection info for the collector's monitored database
     2. Execute check.CheckQuery against the monitored database
     3. Compare result with expected_result
     4. Build HealthCheckResult with passed status
   - For now, store results in a simple results table or return directly
   - Handle query errors gracefully (return as failed check)

4. GetAllHealthChecks(ctx context.Context) ([]*models.VersionHealthCheck, error)
   - Query: SELECT * FROM postgres_health_checks ORDER BY min_version, check_name
   - Return all defined health checks (admin endpoint)

Follow the prepared statement pattern from version_store.go with proper error handling.</action>
  <verify>
    <automated>go build ./backend/internal/storage</automated>
  </verify>
  <done>
    - version_health_store.go exists with 4 methods
    - GetHealthChecksForVersion filters by version range
    - RunHealthCheck executes check query
    - Package compiles successfully
  </done>
</task>

<task type="auto">
  <name>Task 4: Create version health check API handlers</name>
  <files>backend/internal/api/handlers_version_health.go</files>
  <read_first>
    - backend/internal/api/handlers_host.go (pattern for API handlers)
    - backend/internal/storage/version_health_store.go (store from Task 3)
    - backend/internal/storage/version_store.go (for version detection)
  </read_first>
  <behavior>
    - Test: GET /api/v1/collectors/:id/health-checks returns 200 with checks array
    - Test: POST /api/v1/collectors/:id/health-checks/run executes checks
    - Test: GET /api/v1/health-checks returns all defined checks
  </behavior>
  <action>Create handlers_version_health.go with the following handlers:

1. handleGetVersionHealthChecks(c *gin.Context)
   - Route: GET /api/v1/collectors/:id/health-checks
   - Parse collector_id from URL
   - Get PostgreSQL version from version_store.GetVersionCapabilities
   - Call store.GetHealthChecksForVersion with pg_version
   - Return VersionHealthCheckResponse with applicable checks
   - Swagger: @Summary Get version-specific health checks for collector

2. handleRunVersionHealthChecks(c *gin.Context)
   - Route: POST /api/v1/collectors/:id/health-checks/run
   - Parse collector_id from URL
   - Get PostgreSQL version
   - Get applicable health checks
   - For each check, call store.RunHealthCheck
   - Aggregate results into HealthCheckSummary
   - Return VersionHealthCheckResponse with results
   - Swagger: @Summary Execute version-specific health checks

3. handleGetAllHealthChecks(c *gin.Context)
   - Route: GET /api/v1/health-checks
   - Call store.GetAllHealthChecks
   - Return array of all defined health checks
   - Query param: ?version=14 to filter by version
   - Swagger: @Summary Get all defined health checks

4. handleGetHealthCheckByID(c *gin.Context)
   - Route: GET /api/v1/health-checks/:id
   - Parse check_id from URL
   - Call store.GetHealthCheckByID
   - Return single health check definition
   - Swagger: @Summary Get health check definition

Use the same pattern as handlers_host.go with proper error handling.</action>
  <verify>
    <automated>go build ./backend/internal/api</automated>
  </verify>
  <done>
    - handlers_version_health.go exists with 4 handlers
    - All handlers have Swagger annotations
    - Error handling returns appropriate HTTP status codes
    - Package compiles successfully
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire version health check routes in server</name>
  <files>backend/internal/api/server.go</files>
  <read_first>
    - backend/internal/api/server.go (current route registration)
    - backend/internal/api/handlers_version_health.go (handlers from Task 4)
  </read_first>
  <behavior>
    - Test: Health check routes are registered under /collectors/:id/health-checks
    - Test: Routes use auth middleware
    - Test: GET /api/v1/health-checks route exists
  </behavior>
  <action>Add the following routes to server.go in the setupRoutes() function:

1. Under collectors group (after existing collector routes):
```go
// Version-specific health checks
collectors.GET("/:id/health-checks", s.handleGetVersionHealthChecks)
collectors.POST("/:id/health-checks/run", s.handleRunVersionHealthChecks)
```

2. Under api group (admin endpoints):
```go
// Health check definitions
api.GET("/health-checks", s.handleGetAllHealthChecks)
api.GET("/health-checks/:id", s.handleGetHealthCheckByID)
```

Place these after the existing classification routes. Use the same authentication pattern as existing routes.</action>
  <verify>
    <automated>grep -c "health-checks" backend/internal/api/server.go</automated>
  </verify>
  <done>
    - 4 health check routes registered in server.go
    - Routes placed under /collectors/:id/health-checks and /health-checks
    - go build ./backend/internal/api passes
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Backend compiles: `go build ./backend/...`
2. Version health models exist with proper structures
3. Migration creates postgres_health_checks table with seed data
4. Store methods filter checks by PostgreSQL version
5. API handlers return version-appropriate checks
6. Routes registered with authentication
</verification>

<success_criteria>
1. User can view version-specific health checks via GET /api/v1/collectors/:id/health-checks
2. Checks adapt based on PostgreSQL version (11-17)
3. EOL versions (11-12) show upgrade warnings
4. Active versions show performance and configuration checks
5. Health check results include severity and remediation suggestions
</success_criteria>

<output>
After completion, create `.planning/phases/11-data-classification-health-analysis/11-04-SUMMARY.md`
</output>