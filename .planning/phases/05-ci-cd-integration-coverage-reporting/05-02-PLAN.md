---
phase: 05-ci-cd-integration-coverage-reporting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .golangci.yml
autonomous: true
requirements:
  - QUAL-06

must_haves:
  truths:
    - "Unused code is detected and flagged in CI"
    - "Developers can see unused imports, variables, and functions in lint output"
    - "CI fails when unused code is present"
  artifacts:
    - path: ".golangci.yml"
      provides: "Unused code detection configuration"
      contains: "unused"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".golangci.yml"
      via: "golangci-lint-action"
      pattern: "golangci-lint-action"
---

<objective>
Enable unused code detection in CI pipeline to enforce code cleanliness.

Purpose: Detect and prevent unused code (imports, variables, functions, types) from accumulating in the codebase, improving maintainability and reducing technical debt.

Output: Modified golangci.yml with unused linter enabled.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-cd-integration-coverage-reporting/05-RESEARCH.md

<interfaces>
<!-- Current golangci.yml configuration -->

From .golangci.yml:
- Version: 2
- Timeout: 5m
- Enabled linters: govet, ineffassign, misspell
- Disabled linters includes: unused (line 22)
- Skip directories: tools, backend/tests, backend/cmd

Key findings from RESEARCH.md:
- golangci-lint v2.11.4 already installed
- `unused` linter detects: unused constants, variables, functions, types
- Current status: 17 unused fields/functions in placeholder implementations
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enable unused linter and document cleanup requirements</name>
  <files>.golangci.yml</files>

  <read_first>
    - .golangci.yml (current configuration)
    - .planning/phases/05-ci-cd-integration-coverage-reporting/05-RESEARCH.md (unused linter documentation)
  </read_first>

  <action>
    Modify `.golangci.yml` to enable the `unused` linter:

    **In the `linters.enable` section, add:**
    ```yaml
    - unused         # Detect unused constants, variables, functions, types
    ```

    **Remove from `linters.disable` section:**
    Remove the line `- unused # 17 unused field/function warnings (placeholder implementations)`

    **Update the issues comment** at the bottom to reflect that unused is now enabled:
    Update the comment to show that unused detection is now active and those 17 warnings must be addressed.

    **Context from research:**
    The current codebase has 17 unused field/function warnings identified as "placeholder implementations."
    Enabling this linter will surface these issues in CI.

    **Expected behavior:**
    - CI will now fail when unused code is present
    - Developers will see specific unused code locations in lint output
    - Forces cleanup of dead code before merging

    **Important:**
    This task ENABLES the linter. Cleanup of existing unused code is a separate concern.
    The linter will surface issues that need to be addressed in subsequent work.

    **Do NOT:**
    - Do NOT add unused to both enable AND disable sections
    - Do NOT change timeout or other configuration values
    - Do NOT modify the skip-dirs configuration
  </action>

  <verify>
    <automated>grep -q "unused" .golangci.yml && ! grep -q "^    - unused.*#" .golangci.yml | grep -c "disabled"</automated>
  </verify>

  <done>
    - `unused` linter added to enabled linters list
    - `unused` removed from disabled linters list
    - Comment updated to reflect enabled status
    - golangci.yml syntax valid
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify unused linter is active in CI</name>
  <files>.golangci.yml</files>

  <read_first>
    - .golangci.yml (modified configuration)
  </read_first>

  <action>
    Run golangci-lint locally to verify the unused linter is now active:

    ```bash
    cd backend && golangci-lint run --timeout=5m 2>&1 | head -50
    ```

    **Expected output:**
    - Linter will report unused code locations (if any exist)
    - Output format: `file.go:line:col: message (unused)`
    - May show some of the 17 previously documented unused items

    **This is informational:**
    The purpose is to verify the linter is WORKING, not necessarily passing.
    If unused code exists, the linter will report it.

    **If linter passes:** No unused code detected (great!)
    **If linter reports issues:** Expected - these are the items to clean up

    **Document findings:**
    Note in the summary how many unused code warnings the linter reports.
    This establishes the cleanup baseline.

    **Do NOT:**
    - Do NOT fail the task if unused code is found (this is expected)
    - Do NOT modify source code to fix unused items (separate concern)
  </action>

  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/backend && golangci-lint run --timeout=5m --disable-all --enable=unused ./... 2>&1 | head -20; echo "Exit code: $?"</automated>
  </verify>

  <done>
    - golangci-lint command executes successfully
    - Unused linter is active (may report issues or pass)
    - Baseline count of unused code items documented
  </done>
</task>

</tasks>

<verification>
**Quality Gate Checks:**
- [ ] `unused` linter in enabled list in .golangci.yml
- [ ] `unused` linter removed from disabled list
- [ ] golangci-lint run succeeds (may have warnings, but no errors)
- [ ] grep confirms unused in enabled section
</verification>

<success_criteria>
1. Unused linter enabled in golangci.yml configuration
2. CI lint job will detect unused code on every run
3. Unused code warnings surface in CI output
4. Developers receive feedback on unused code in PRs
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-cd-integration-coverage-reporting/05-02-SUMMARY.md`
</output>