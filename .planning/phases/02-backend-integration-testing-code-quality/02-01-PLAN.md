---
phase: 02-backend-integration-testing-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.golangci.yml, .pre-commit-config.yaml]
autonomous: true
requirements: [QUAL-01, QUAL-03]
user_setup:
  - service: gitleaks
    why: "Secret scanning for hardcoded credentials"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Developer can run golangci-lint and get consistent results"
    - "Codebase passes lint checks with zero warnings"
    - "Security scan finds zero hardcoded secrets"
  artifacts:
    - path: ".golangci.yml"
      provides: "Consistent linting configuration"
      min_lines: 30
    - path: ".pre-commit-config.yaml"
      provides: "Automated lint and secret scanning hooks"
  key_links:
    - from: ".golangci.yml"
      to: "golangci-lint binary"
      via: "configuration file"
      pattern: "linters:"

---

<objective>
Establish code quality infrastructure with golangci-lint configuration and gitleaks secret scanning.

Purpose: Ensure consistent code quality checks across all development environments and prevent hardcoded secrets from entering the codebase.

Output: Working linting configuration, secret scanning setup, and pre-commit hooks for automated quality gates.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-integration-testing-code-quality/02-RESEARCH.md

<interfaces>
<!-- Existing Makefile commands for verification -->
From Makefile:
- `make lint` - runs golangci-lint
- `make fmt` - runs go fmt
- `make test-backend` - runs tests with coverage

<!-- Research recommendations -->
From 02-RESEARCH.md:
- golangci-lint v2.11.4 already installed
- Recommended linters: errcheck, gosimple, govet, ineffassign, staticcheck, unused, bodyclose, gosec, gofmt, goimports, misspell, revive, sqlclosecheck
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golangci-lint configuration</name>
  <files>.golangci.yml</files>
  <read_first>
    - /Users/glauco.torres/git/pganalytics-v3/backend/go.mod (verify Go version)
    - /Users/glauco.torres/git/pganalytics-v3/Makefile (check existing lint command)
  </read_first>
  <action>Create `.golangci.yml` at project root with the following configuration:

```yaml
run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck      # Check for unchecked errors
    - gosimple      # Simplify code
    - govet         # Report suspicious constructs
    - ineffassign   # Detect unused assignments
    - staticcheck   # Go static analysis
    - unused        # Find unused code
    - bodyclose     # Check HTTP response body is closed
    - gosec         # Security checks
    - gofmt         # Check formatting
    - goimports     # Check imports
    - misspell      # Find misspelled words
    - revive        # General purpose linter
    - sqlclosecheck # Check sql.Rows and sql.Stmt are closed

linters-settings:
  gosec:
    excludes:
      - G104 # Audit errors (will be handled by errcheck)
  errcheck:
    check-type-assertions: true
    check-blank: true
  govet:
    enable-all: true

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
```

This configuration is identical to the one recommended in 02-RESEARCH.md.</action>
  <verify>
    <automated>test -f /Users/glauco.torres/git/pganalytics-v3/.golangci.yml && grep -q "linters:" /Users/glauco.torres/git/pganalytics-v3/.golangci.yml</automated>
  </verify>
  <done>.golangci.yml exists with linters section containing at least errcheck, gosec, govet, staticcheck</done>
</task>

<task type="auto">
  <name>Task 2: Run initial lint scan and fix issues</name>
  <files>backend/**/*.go</files>
  <read_first>
    - /Users/glauco.torres/git/pganalytics-v3/.golangci.yml
    - /Users/glauco.torres/git/pganalytics-v3/backend/go.mod
  </read_first>
  <action>Run the linting scan to identify any existing issues:

1. Run `cd /Users/glauco.torres/git/pganalytics-v3/backend && golangci-lint run ./...` to see current issues
2. If issues are found, categorize them:
   - Formatting issues: fix with `go fmt ./...`
   - Unused imports: remove with `goimports -local github.com/torresglauco/pganalytics-v3 -w .`
   - Simple fixes: address directly
   - Complex issues: document for later review
3. Re-run lint until zero warnings or document remaining issues that are acceptable exceptions

The goal is `golangci-lint run ./...` returns exit code 0.</action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/backend && golangci-lint run ./...; echo "Exit code: $?"</automated>
  </verify>
  <done>golangci-lint run ./... exits with code 0 (no warnings)</done>
</task>

<task type="auto">
  <name>Task 3: Install and configure gitleaks</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - /Users/glauco.torres/git/pganalytics-v3/.gitignore
  </read_first>
  <action>Set up secret scanning infrastructure:

1. Install gitleaks using Homebrew:
   ```bash
   brew install gitleaks
   ```

2. Run initial scan to verify no secrets exist:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3
   gitleaks detect --source . --verbose
   ```
   The scan should return exit code 0 (no leaks found) since Phase 1 already fixed credential issues.

3. Create `.pre-commit-config.yaml` for automated scanning:
   ```yaml
   repos:
     - repo: https://github.com/golangci/golangci-lint
       rev: v2.11.4
       hooks:
         - id: golangci-lint
           entry: golangci-lint run --new-from-rev=HEAD~1
           types: [go]
           language: system

     - repo: https://github.com/gitleaks/gitleaks
       rev: v8.18.0
       hooks:
         - id: gitleaks
           args: ['--staged']
   ```

4. Verify pre-commit hooks work:
   ```bash
   pre-commit install
   ```

Note: Pre-commit framework may need installation via `brew install pre-commit` if not already present.</action>
  <verify>
    <automated>which gitleaks && gitleaks detect --source /Users/glauco.torres/git/pganalytics-v3 --no-git -v 2>&1 | head -20; echo "Exit code: $?"</automated>
  </verify>
  <done>gitleaks installed, pre-commit config exists, gitleaks scan returns exit code 0 (no secrets found)</done>
</task>

</tasks>

<verification>
1. Run `cd /Users/glauco.torres/git/pganalytics-v3/backend && golangci-lint run ./...` - must return exit code 0
2. Run `gitleaks detect --source . --verbose` - must return exit code 0 (no leaks)
3. Verify `.golangci.yml` exists with required linters
4. Verify `.pre-commit-config.yaml` exists with gitleaks hook
</verification>

<success_criteria>
1. golangci-lint configuration file exists at project root with linters enabled
2. `make lint` passes with zero warnings (exit code 0)
3. gitleaks is installed and available in PATH
4. gitleaks scan shows zero hardcoded secrets in codebase
5. Pre-commit configuration exists for automated lint and secret scanning
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration-testing-code-quality/02-01-SUMMARY.md`
</output>