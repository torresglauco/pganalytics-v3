---
phase: 04-final-integration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/BulkRuleActions.tsx
  - frontend/src/components/ManagedInstancesTable.tsx
  - frontend/src/components/UserManagementTable.tsx
  - frontend/src/pages/Dashboard.test.tsx
  - frontend/src/components/tables/DataTable.test.tsx
  - frontend/src/components/CollectorList.test.tsx
autonomous: true
requirements:
  - QUAL-02
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "npm run lint in frontend/ returns exit code 0"
    - "Zero ESLint errors in the codebase"
  artifacts:
    - path: "frontend/src/components/BulkRuleActions.tsx"
      provides: "Bulk rule actions with AlertCircle icon"
      contains: "import.*AlertCircle.*from 'lucide-react'"
    - path: "frontend/src/components/ManagedInstancesTable.tsx"
      provides: "Instances table with AlertCircle icon"
      contains: "import.*AlertCircle.*from 'lucide-react'"
    - path: "frontend/src/components/UserManagementTable.tsx"
      provides: "User management with User icon"
      contains: "import.*User.*from 'lucide-react'"
    - path: "frontend/src/pages/Dashboard.test.tsx"
      provides: "Dashboard tests with proper apiClient mock"
      contains: "const apiClient = vi.importActual"
    - path: "frontend/src/components/tables/DataTable.test.tsx"
      provides: "DataTable tests without unused imports"
      min_lines: 1
    - path: "frontend/src/components/CollectorList.test.tsx"
      provides: "CollectorList tests without unused variables"
      min_lines: 1
  key_links:
    - from: "BulkRuleActions.tsx"
      to: "lucide-react"
      via: "import"
      pattern: "AlertCircle.*lucide-react"
    - from: "Dashboard.test.tsx"
      to: "services/api"
      via: "vi.mock"
      pattern: "apiClient"
---

<objective>
Fix remaining 26 ESLint errors to achieve exit code 0 for QUAL-02.

Purpose: Complete code quality requirement QUAL-02 by resolving all blocking ESLint errors.
Output: Zero ESLint errors, exit code 0 from `npm run lint` in frontend/.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-final-integration/04-VERIFICATION.md

## Error Categories (26 total)

### Category 1: Missing lucide-react imports (3 errors)
- BulkRuleActions.tsx:103 - AlertCircle not defined
- ManagedInstancesTable.tsx:407 - AlertCircle not defined
- UserManagementTable.tsx:355 - User not defined

### Category 2: Missing apiClient mock/import (7 errors)
- Dashboard.test.tsx lines 67,102,115,126,134,144,153 - apiClient not defined

### Category 3: Unused imports/variables (16 errors)
- DataTable.test.tsx: unused vi, beforeEach, fireEvent
- CollectorList.test.tsx: unused waitFor, deleteError variable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing lucide-react icon imports</name>
  <files>
    frontend/src/components/BulkRuleActions.tsx,
    frontend/src/components/ManagedInstancesTable.tsx,
    frontend/src/components/UserManagementTable.tsx
  </files>
  <read_first>
    frontend/src/components/BulkRuleActions.tsx (lines 1-5),
    frontend/src/components/ManagedInstancesTable.tsx (lines 1-5),
    frontend/src/components/UserManagementTable.tsx (lines 1-5)
  </read_first>
  <action>
    Add missing icon imports to existing lucide-react import statements:

    1. **BulkRuleActions.tsx line 2:**
       - Current: `import { ChevronDown } from 'lucide-react';`
       - Change to: `import { ChevronDown, AlertCircle } from 'lucide-react';`

    2. **ManagedInstancesTable.tsx line 2:**
       - Current: `import { Trash2, Plus, X, CheckCircle, Edit, Zap } from 'lucide-react'`
       - Change to: `import { Trash2, Plus, X, CheckCircle, Edit, Zap, AlertCircle } from 'lucide-react'`

    3. **UserManagementTable.tsx line 2:**
       - Current: `import { Trash2, Lock, Unlock, Shield, RotateCcw, Copy, CheckCircle, Plus, X } from 'lucide-react'`
       - Change to: `import { Trash2, Lock, Unlock, Shield, RotateCcw, Copy, CheckCircle, Plus, X, User } from 'lucide-react'`

    These components use the icons in JSX (AlertCircle for error/empty states, User for user role toggle button).
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -c "AlertCircle.*is not defined\|User.*is not defined" || echo "0"</automated>
    Expected output: "0" (no missing icon errors)
  </verify>
  <done>
    All three components have the required icon imports added to their lucide-react import statements.
    ESLint no longer reports "AlertCircle is not defined" or "User is not defined" errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix apiClient reference in Dashboard.test.tsx</name>
  <files>
    frontend/src/pages/Dashboard.test.tsx
  </files>
  <read_first>
    frontend/src/pages/Dashboard.test.tsx (lines 1-70)
  </read_first>
  <action>
    The test file mocks apiClient but doesn't import it for use in type assertions.

    Add this import after line 5 (after the User type import):
    ```typescript
    import { apiClient } from '../services/api'
    ```

    The mock at lines 8-12 creates the mock implementation. The import allows the test code to reference `apiClient` for type assertions like `(apiClient.getCurrentUser as ReturnType<typeof vi.fn>).mockReturnValue(...)`.

    Alternative approach (if import causes issues with the mock): Use `vi.importActual` pattern or reference the mock directly through the mocked module. However, the simplest fix is to import the mocked apiClient - Vitest will use the mock implementation due to `vi.mock` being called first.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -c "apiClient.*is not defined" || echo "0"</automated>
    Expected output: "0" (no apiClient undefined errors)
  </verify>
  <done>
    Dashboard.test.tsx properly imports apiClient (which uses the mocked implementation).
    ESLint no longer reports "apiClient is not defined" errors on lines 67, 102, 115, 126, 134, 144, 153.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove unused imports and variables from test files</name>
  <files>
    frontend/src/components/tables/DataTable.test.tsx,
    frontend/src/components/CollectorList.test.tsx
  </files>
  <read_first>
    frontend/src/components/tables/DataTable.test.tsx (full file),
    frontend/src/components/CollectorList.test.tsx (lines 1-10, 200-228)
  </read_first>
  <action>
    Remove unused imports and variables:

    1. **DataTable.test.tsx line 1:**
       - Current: `import { describe, it, expect, vi, beforeEach } from 'vitest';`
       - Change to: `import { describe, it, expect } from 'vitest';`
       (vi and beforeEach are imported but never used in this file)

    2. **DataTable.test.tsx line 2:**
       - Current: `import { render, screen, fireEvent, waitFor } from '@testing-library/react';`
       - Change to: `import { render, screen, waitFor } from '@testing-library/react';`
       (fireEvent is imported but never used - the file uses userEvent instead)

    3. **CollectorList.test.tsx line 2:**
       - Current: `import { screen, waitFor } from '@testing-library/react'`
       - Change to: `import { screen } from '@testing-library/react'`
       (waitFor is imported but never used in this file)

    4. **CollectorList.test.tsx line 208:**
       - Remove the unused `deleteError` variable declaration (lines 208-211).
       - The variable is assigned but never used in the test assertion.
       - Delete these lines:
         ```typescript
         const deleteError = {
           message: 'Cannot delete collector with active metrics',
           status_code: 400,
         }
         ```
       - The test at line 207 "should show delete error message" currently just checks the structure exists (line 226) and doesn't actually use deleteError. Remove the unused variable declaration.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -c "'vi' is defined but never used\|'beforeEach' is defined but never used\|'fireEvent' is defined but never used\|'waitFor' is defined but never used\|'deleteError' is defined but never used" || echo "0"</automated>
    Expected output: "0" (no unused variable errors)
  </verify>
  <done>
    DataTable.test.tsx has only the imports it actually uses (describe, it, expect, render, screen, waitFor).
    CollectorList.test.tsx has only the imports it uses and no unused variable declarations.
    ESLint no longer reports unused variable errors for these files.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify ESLint passes with exit code 0</name>
  <files>
    None (verification only)
  </files>
  <read_first>
    None
  </read_first>
  <action>
    Run the full ESLint check and verify exit code 0:

    ```bash
    cd frontend && npm run lint
    ```

    The expected result is:
    - Exit code: 0 (success)
    - Errors: 0
    - Warnings: ~161 (no-explicit-any warnings are acceptable - not blocking)

    If any errors remain, analyze the output and identify what additional fixes are needed.
  </action>
  <verify>
    <automated>cd frontend && npm run lint; echo "EXIT_CODE=$?"</automated>
    Expected output: Exit code 0, zero errors reported
  </verify>
  <done>
    `npm run lint` in frontend/ returns exit code 0.
    QUAL-02 requirement is fully satisfied.
    The codebase has zero ESLint errors (warnings are acceptable).
  </done>
</task>

</tasks>

<verification>
Run the following to verify all gaps are closed:

```bash
# Verify ESLint passes with exit code 0
cd frontend && npm run lint

# Verify zero errors (warnings are OK)
cd frontend && npm run lint 2>&1 | grep -E "✖ [0-9]+ errors" || echo "No errors found"

# Verify specific files are fixed
cd frontend && npm run lint -- src/components/BulkRuleActions.tsx src/components/ManagedInstancesTable.tsx src/components/UserManagementTable.tsx src/pages/Dashboard.test.tsx src/components/tables/DataTable.test.tsx src/components/CollectorList.test.tsx
```
</verification>

<success_criteria>
QUAL-02 requirement fully satisfied:
1. `npm run lint` in frontend/ returns exit code 0
2. Zero ESLint errors in the codebase
3. All 26 errors from VERIFICATION.md resolved:
   - 3 missing lucide-react imports added
   - 7 apiClient references fixed
   - 16 unused imports/variables removed
</success_criteria>

<output>
After completion, create `.planning/phases/04-final-integration/04-07-SUMMARY.md`
</output>