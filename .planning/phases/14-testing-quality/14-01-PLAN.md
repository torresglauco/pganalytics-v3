---
phase: 14-testing-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/middleware/tenant_context_test.go
  - backend/internal/services/health_score_calculator_test.go
  - collector/tests/unit/data_classification_test.cpp
  - collector/tests/unit/host_metrics_test.cpp
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "All new collector plugins have C++ unit tests passing"
    - "All new backend services have Go unit tests passing"
    - "Health score calculation produces correct values for given inputs"
    - "Tenant context middleware correctly isolates tenant data"
    - "Data classification pattern detection validates CPF, CNPJ, and credit card numbers"
  artifacts:
    - path: "backend/internal/middleware/tenant_context_test.go"
      provides: "Tenant middleware unit tests"
      min_lines: 80
    - path: "backend/internal/services/health_score_calculator_test.go"
      provides: "Health score calculation tests"
      min_lines: 100
    - path: "collector/tests/unit/data_classification_test.cpp"
      provides: "Data classification plugin tests"
      min_lines: 150
    - path: "collector/tests/unit/host_metrics_test.cpp"
      provides: "Host metrics collection tests"
      min_lines: 100
  key_links:
    - from: "backend/internal/middleware/tenant_context_test.go"
      to: "backend/internal/middleware/tenant_context.go"
      via: "import"
      pattern: "TenantContextMiddleware"
    - from: "collector/tests/unit/data_classification_test.cpp"
      to: "collector/include/data_classification_plugin.h"
      via: "include"
      pattern: "DataClassification"
---

<objective>
Create comprehensive unit tests for backend services and collector plugins added in Phases 10-12.

Purpose: Ensure core business logic and data processing work correctly in isolation.
Output: Unit test files with 80%+ coverage on new code paths.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-testing-quality/14-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing codebase -->

From backend/internal/middleware/tenant_context.go:
```go
func TenantContextMiddleware(store *storage.PostgresDB, logger *zap.Logger) gin.HandlerFunc
func GetTenantIDFromContext(c *gin.Context) (uuid.UUID, bool)
func RequireTenant() gin.HandlerFunc
```

From backend/internal/services/health_score_calculator.go:
```go
// Health score calculation with weighted components
// CPU: 30%, Memory: 25%, Disk: 25%, Load: 20%
// Score ranges: 0-100, Status: healthy(>=80), degraded(>=60), warning(>=40), critical(<40)
```

From backend/internal/auth/service_test.go (existing pattern):
```go
type MockUserStore struct {
    users map[string]*models.User
}
func NewMockUserStore() *MockUserStore
// Pattern: Create mock stores, inject into service, test methods
```

From collector/tests/unit/replication_collector_test.cpp (existing pattern):
```cpp
class ReplicationCollectorTest : public ::testing::Test {
protected:
    void SetUp() override { /* init test fixtures */ }
};
TEST_F(ReplicationCollectorTest, ConstructorInitializesCorrectly) {
    // Use EXPECT_EQ, EXPECT_TRUE for assertions
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tenant context middleware unit tests</name>
  <files>
    backend/internal/middleware/tenant_context_test.go
  </files>
  <read_first>
    - backend/internal/middleware/tenant_context.go
    - backend/internal/auth/service_test.go (for mock pattern)
  </read_first>
  <behavior>
    - Test: TenantContextMiddleware skips when no user_id in context (public endpoints)
    - Test: TenantContextMiddleware returns 500 when user_id has wrong type
    - Test: TenantContextMiddleware returns 403 when user has no tenant
    - Test: TenantContextMiddleware sets tenant_id and tenant_slug in gin context on success
    - Test: GetTenantIDFromContext extracts uuid correctly
    - Test: RequireTenant middleware returns 403 when no tenant_id set
  </behavior>
  <action>
    Create tenant_context_test.go with testify assertions. Follow the MockUserStore pattern from auth/service_test.go. Create a MockTenantStore that implements GetTenantByUserID and SetTenantSessionVariable. Test all middleware branches: no user_id (skip), invalid type (500), no tenant (403), success path. Use httptest.NewRecorder for HTTP response testing. Use gin.CreateTestContext for context setup.
  </action>
  <verify>
    <automated>go test ./internal/middleware/... -v -run TestTenant</automated>
  </verify>
  <done>
    All 6 test cases pass, 80%+ coverage on tenant_context.go, mock store pattern followed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create health score calculator unit tests</name>
  <files>
    backend/internal/services/health_score_calculator_test.go
  </files>
  <read_first>
    - backend/internal/services/health_score_calculator.go
    - backend/pkg/models/health_models.go
  </read_first>
  <behavior>
    - Test: Score 100 when all metrics are optimal (CPU 0%, Memory 0%, Disk 0%, Load 0)
    - Test: Score 0 when all metrics are at critical thresholds
    - Test: Weighted calculation: CPU 30% + Memory 25% + Disk 25% + Load 20%
    - Test: Status "healthy" when score >= 80
    - Test: Status "degraded" when score >= 60 and < 80
    - Test: Status "warning" when score >= 40 and < 60
    - Test: Status "critical" when score < 40
    - Test: Component scores stored in result structure
    - Test: CalculationDetails JSONB contains formula breakdown
  </behavior>
  <action>
    Create health_score_calculator_test.go. Test the weighted health score formula with known inputs and expected outputs. Create test cases for boundary conditions (0, 40, 60, 80, 100). Test status label assignment. Verify component scores are calculated and stored correctly. Use table-driven tests for different metric combinations. Reference the formula: CPU 30%, Memory 25%, Disk 25%, Load 20%.
  </action>
  <verify>
    <automated>go test ./internal/services/... -v -run TestHealth</automated>
  </verify>
  <done>
    All 9 test cases pass, weighted formula verified, status boundaries correct, 80%+ coverage
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create collector data classification plugin tests</name>
  <files>
    collector/tests/unit/data_classification_test.cpp
  </files>
  <read_first>
    - collector/tests/unit/replication_collector_test.cpp (for pattern)
    - collector/include/host_inventory_plugin.h (if exists)
  </read_first>
  <behavior>
    - Test: CPF validation with valid check digits (e.g., 123.456.789-09)
    - Test: CPF validation rejects invalid check digits
    - Test: CNPJ validation with valid format
    - Test: CNPJ validation rejects invalid format
    - Test: Email pattern detection with valid emails
    - Test: Phone pattern detection for Brazilian format
    - Test: Credit card Luhn algorithm validation
    - Test: Credit card type detection (Visa, Mastercard, etc.)
    - Test: Pattern detection returns correct JSON structure
  </behavior>
  <action>
    Create data_classification_test.cpp using Google Test framework. Follow the pattern from replication_collector_test.cpp. Test pattern validation functions for CPF, CNPJ, email, phone, and credit cards. Test Luhn algorithm implementation. Use known test values with verified check digits. Create a test fixture class with SetUp for common initialization. Mark tests requiring PostgreSQL connection as DISABLED_ with GTEST_SKIP for CI environments.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/collector && cmake -B build && cmake --build build && cd build && ctest -R data_classification -V</automated>
  </verify>
  <done>
    All pattern validation tests pass, Luhn algorithm verified, JSON structure validated, builds with CMake
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create collector host metrics plugin tests</name>
  <files>
    collector/tests/unit/host_metrics_test.cpp
  </files>
  <read_first>
    - collector/tests/unit/replication_collector_test.cpp (for pattern)
    - collector/src/host_inventory_plugin.cpp
  </read_first>
  <behavior>
    - Test: Host metrics collector initializes with correct parameters
    - Test: CPU metrics parsing from /proc/stat format
    - Test: Memory metrics parsing from /proc/meminfo format
    - Test: Disk metrics parsing from statvfs output
    - Test: Network I/O metrics parsing
    - Test: Metrics JSON output contains required fields (cpu_user, cpu_system, memory_used, disk_used, etc.)
    - Test: Handle missing /proc files gracefully (return zeros or error in JSON)
  </behavior>
  <action>
    Create host_metrics_test.cpp using Google Test. Test the host inventory plugin's metric collection functions. Create mock or sample data for /proc parsing. Test that the output JSON contains all required fields. Test error handling when system files are unavailable. Follow the DISABLED_ pattern for tests requiring actual system access in CI.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/collector && cmake -B build && cmake --build build && cd build && ctest -R host_metrics -V</automated>
  </verify>
  <done>
    All metric parsing tests pass, JSON structure verified, error handling tested, builds with CMake
  </done>
</task>

</tasks>

<verification>
1. Run all backend unit tests: `go test ./internal/middleware/... ./internal/services/... -v`
2. Run all collector unit tests: `cd collector && ctest --output-on-failure`
3. Verify coverage: `go test ./internal/... -coverprofile=coverage.out && go tool cover -func=coverage.out`
4. All tests must pass with 80%+ coverage on new code
</verification>

<success_criteria>
- [ ] backend/internal/middleware/tenant_context_test.go exists with 6+ test cases
- [ ] backend/internal/services/health_score_calculator_test.go exists with 9+ test cases
- [ ] collector/tests/unit/data_classification_test.cpp exists with 9+ test cases
- [ ] collector/tests/unit/host_metrics_test.cpp exists with 7+ test cases
- [ ] All Go tests pass: `go test ./internal/middleware/... ./internal/services/...`
- [ ] All C++ tests pass: `cd collector/build && ctest`
- [ ] Coverage >= 80% on new code paths
</success_criteria>

<output>
After completion, create `.planning/phases/14-testing-quality/14-01-SUMMARY.md`
</output>