---
phase: 13-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/types/replication.ts
  - frontend/src/api/replicationApi.ts
  - frontend/src/components/topology/TopologyGraph.tsx
  - frontend/src/components/topology/TopologyNode.tsx
  - frontend/src/components/topology/TopologyEdge.tsx
  - frontend/src/components/topology/TopologyLegend.tsx
  - frontend/src/pages/ReplicationTopologyPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - REP-06
  - UI-01
user_setup: []

must_haves:
  truths:
    - "User can view replication topology as an interactive graph"
    - "User can see primary and standby nodes with role indicators"
    - "User can see replication lag displayed on edges"
    - "User can zoom and pan the topology graph"
    - "User can see node status (streaming, catchup, down)"
  artifacts:
    - path: "frontend/src/components/topology/TopologyGraph.tsx"
      provides: "Interactive graph visualization using @xyflow/react"
      min_lines: 50
    - path: "frontend/src/pages/ReplicationTopologyPage.tsx"
      provides: "Page component with loading state and data fetching"
      exports: ["ReplicationTopologyPage"]
    - path: "frontend/src/api/replicationApi.ts"
      provides: "API client for replication endpoints"
      exports: ["replicationApi"]
  key_links:
    - from: "frontend/src/pages/ReplicationTopologyPage.tsx"
      to: "/api/v1/collectors/{id}/topology"
      via: "replicationApi.getTopology"
      pattern: "replicationApi\\.getTopology"
    - from: "frontend/src/components/topology/TopologyGraph.tsx"
      to: "frontend/src/pages/ReplicationTopologyPage.tsx"
      via: "props: topology data"
      pattern: "topology.*TopologyGraph"
---

<objective>
Implement interactive replication topology visualization using @xyflow/react (v12.x).

Purpose: Enable users to visualize cascading replication topology (primary -> standby -> standby) as an interactive graph with lag metrics and status indicators.
Output: ReplicationTopologyPage with custom nodes, edges, and legend components.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-ui/13-RESEARCH.md
@.planning/phases/13-frontend-ui/13-VALIDATION.md

<interfaces>
<!-- Backend API response types - Executor should use these directly -->

From backend/pkg/models/replication_models.go:
```go
type ReplicationTopology struct {
    CollectorID         uuid.UUID      `json:"collector_id"`
    NodeRole            string         `json:"node_role"` // primary, standby, cascading_standby
    UpstreamCollectorID *uuid.UUID     `json:"upstream_collector_id,omitempty"`
    UpstreamHost        string         `json:"upstream_host,omitempty"`
    UpstreamPort        int            `json:"upstream_port,omitempty"`
    DownstreamCount     int            `json:"downstream_count"`
    DownstreamNodes     []TopologyNode `json:"downstream_nodes,omitempty"`
}

type TopologyNode struct {
    CollectorID     uuid.UUID `json:"collector_id"`
    ApplicationName string    `json:"application_name"`
    ClientAddr      string    `json:"client_addr"`
    State           string    `json:"state"`
    SyncState       string    `json:"sync_state"`
    ReplayLagMs     int64     `json:"replay_lag_ms"`
}
```

API Endpoint: GET /api/v1/collectors/{id}/topology
Response: `{ "topology": ReplicationTopology }`

From frontend/src/api/alertDashboardApi.ts pattern:
```typescript
const API_BASE = '/api/v1';
async function apiCall<T>(endpoint: string, options: RequestInit = {}): Promise<T> {
  const response = await fetch(`${API_BASE}${endpoint}`, {
    ...options,
    credentials: 'include',
    headers: { 'Content-Type': 'application/json', ...options.headers },
  });
  if (!response.ok) throw new Error(...);
  return response.json();
}
```

From frontend/src/pages/AlertsDashboard.tsx pattern:
- Use useState for data, isLoading, error states
- Use useEffect for data fetching on mount
- Handle loading with LoadingSpinner component
- Handle errors with error message display
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @xyflow/react and create type definitions</name>
  <files>frontend/package.json, frontend/src/types/replication.ts</files>
  <read_first>
    - frontend/package.json
    - backend/pkg/models/replication_models.go
  </read_first>
  <acceptance_criteria>
    - grep -q "@xyflow/react" frontend/package.json
    - grep -q "ReplicationTopology" frontend/src/types/replication.ts
    - grep -q "TopologyNode" frontend/src/types/replication.ts
  </acceptance_criteria>
  <action>
Install @xyflow/react dependencies and create TypeScript type definitions that mirror backend models.

1. Install dependencies:
```bash
cd frontend && npm install @xyflow/react @xyflow/background @xyflow/controls @xyflow/minimap
```

2. Create frontend/src/types/replication.ts with types matching backend models:
- ReplicationTopology (collector_id, node_role, upstream info, downstream_nodes)
- TopologyNode (collector_id, application_name, client_addr, state, sync_state, replay_lag_ms)
- ReplicationStatus (for replication metrics display)
- ReplicationSlot (for slots display)

CRITICAL: Use @xyflow/react v12.x NOT reactflow v11 - the packages have different scopes and APIs. The project research confirms @xyflow/react 12.10.2 is the current maintained version.
  </action>
  <verify>
    <automated>cd frontend && npm ls @xyflow/react && grep -q "ReplicationTopology" src/types/replication.ts</automated>
  </verify>
  <done>@xyflow/react installed, TypeScript types created matching backend models</done>
</task>

<task type="auto">
  <name>Task 2: Create replication API client</name>
  <files>frontend/src/api/replicationApi.ts</files>
  <read_first>
    - frontend/src/api/alertDashboardApi.ts
    - frontend/src/types/replication.ts
    - backend/internal/api/handlers_replication.go
  </read_first>
  <acceptance_criteria>
    - grep -q "getTopology" frontend/src/api/replicationApi.ts
    - grep -q "getReplicationMetrics" frontend/src/api/replicationApi.ts
    - grep -q "getReplicationSlots" frontend/src/api/replicationApi.ts
  </acceptance_criteria>
  <action>
Create API client for replication endpoints following the existing alertDashboardApi.ts pattern.

Implement replicationApi object with methods:
- getTopology(collectorId: string): Promise<ReplicationTopology>
- getReplicationMetrics(collectorId: string, limit?: number, offset?: number): Promise<ReplicationMetricsResponse>
- getReplicationSlots(collectorId: string, limit?: number, offset?: number): Promise<ReplicationSlot[]>
- getLogicalSubscriptions(collectorId: string, database?: string): Promise<LogicalSubscription[]>
- getPublications(collectorId: string, database?: string): Promise<Publication[]>

Use the apiCall helper pattern from alertDashboardApi.ts:
- API_BASE = '/api/v1'
- credentials: 'include' for cookie-based auth
- Proper error handling with JSON parsing
- Return typed responses

Endpoints:
- GET /api/v1/collectors/{id}/topology
- GET /api/v1/collectors/{id}/replication
- GET /api/v1/collectors/{id}/replication-slots
- GET /api/v1/collectors/{id}/logical-subscriptions
- GET /api/v1/collectors/{id}/publications
  </action>
  <verify>
    <automated>cd frontend && grep -q "getTopology" src/api/replicationApi.ts && grep -q "API_BASE" src/api/replicationApi.ts</automated>
  </verify>
  <done>API client created with all replication endpoint methods</done>
</task>

<task type="auto">
  <name>Task 3: Create topology graph components</name>
  <files>
    frontend/src/components/topology/TopologyGraph.tsx,
    frontend/src/components/topology/TopologyNode.tsx,
    frontend/src/components/topology/TopologyEdge.tsx,
    frontend/src/components/topology/TopologyLegend.tsx
  </files>
  <read_first>
    - frontend/src/types/replication.ts
    - frontend/src/components/ui/LoadingSpinner.tsx
    - frontend/src/components/ui/Badge.tsx
  </read_first>
  <acceptance_criteria>
    - grep -q "ReactFlow" frontend/src/components/topology/TopologyGraph.tsx
    - grep -q "Handle" frontend/src/components/topology/TopologyNode.tsx
    - grep -q "getBezierPath" frontend/src/components/topology/TopologyEdge.tsx
    - test -f frontend/src/components/topology/TopologyLegend.tsx
  </acceptance_criteria>
  <action>
Create topology graph components using @xyflow/react.

TopologyNode.tsx - Custom node component:
- Use Handle components for connection points (Position.Left target, Position.Right source)
- Display role badge (primary=emerald, standby=blue, cascading_standby=amber)
- Display status indicator (streaming=green, catchup=yellow, down=red)
- Show application_name and lag_ms for non-primary nodes
- Use Tailwind CSS for styling (border-2, rounded-lg, shadow-md)

TopologyEdge.tsx - Custom edge component:
- Use getBezierPath for curved edges
- Display lag_ms label on edge
- Color based on lag severity (low=green, medium=yellow, high=red)

TopologyGraph.tsx - Main graph component:
- Import ReactFlow, Background, Controls, MiniMap from @xyflow/react
- Define nodeTypes OUTSIDE component to prevent re-renders (CRITICAL: not inline)
- Convert ReplicationTopology to nodes/edges with dagre-like layout
- Node positions: primary at top center, standbys below, cascading at bottom
- Enable zoom, pan, minimap

TopologyLegend.tsx - Legend component:
- Show role colors (primary, standby, cascading_standby)
- Show status colors (streaming, catchup, down)
- Show lag thresholds
  </action>
  <verify>
    <automated>cd frontend && grep -q "ReactFlow" src/components/topology/TopologyGraph.tsx && grep -q "Handle" src/components/topology/TopologyNode.tsx</automated>
  </verify>
  <done>Topology graph components created with custom nodes, edges, and legend</done>
</task>

<task type="auto">
  <name>Task 4: Create ReplicationTopologyPage and wire routes</name>
  <files>frontend/src/pages/ReplicationTopologyPage.tsx, frontend/src/App.tsx, frontend/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - frontend/src/pages/AlertsDashboard.tsx
    - frontend/src/App.tsx
    - frontend/src/components/layout/Sidebar.tsx
    - frontend/src/api/replicationApi.ts
  </read_first>
  <acceptance_criteria>
    - grep -q "ReplicationTopologyPage" frontend/src/App.tsx
    - grep -q "/topology" frontend/src/App.tsx
    - grep -q "Topology" frontend/src/components/layout/Sidebar.tsx
    - grep -q "ReplicationTopologyPage" frontend/src/pages/ReplicationTopologyPage.tsx
  </acceptance_criteria>
  <action>
Create the page component and wire into routing.

ReplicationTopologyPage.tsx:
- Use useParams to get collectorId from route
- Fetch topology using replicationApi.getTopology(collectorId)
- Handle loading state with LoadingSpinner
- Handle error state with error message display
- Render TopologyGraph with TopologyLegend
- Add refresh button for manual reload
- Show empty state if no topology data

App.tsx routing:
- Add route: `/topology/:collectorId` -> ReplicationTopologyPage
- Place in authenticated routes section

Sidebar.tsx:
- Add "Replication Topology" navigation item with icon (GitBranch or Network)
- Link to /topology route (user selects collector from list)
- Place after Query Performance or near other monitoring items
  </action>
  <verify>
    <automated>cd frontend && grep -q "ReplicationTopologyPage" src/App.tsx && grep -q "topology" src/App.tsx</automated>
  </verify>
  <done>Page created and wired with routing, accessible from sidebar</done>
</task>

</tasks>

<verification>
- Run `cd frontend && npm run type-check` - TypeScript compiles without errors
- Run `cd frontend && npm run lint` - No ESLint errors
- Run `cd frontend && npm test` - Tests pass (or Wave 0 creates test scaffolds)
- Manual: Navigate to /topology/{collectorId} and verify graph renders with nodes and edges
</verification>

<success_criteria>
- User can view replication topology as an interactive graph with zoom/pan
- Primary and standby nodes are visually distinct with role badges
- Replication lag is displayed on edges with color coding
- Page accessible from sidebar navigation
- Loading and error states handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-ui/13-01-SUMMARY.md`
</output>