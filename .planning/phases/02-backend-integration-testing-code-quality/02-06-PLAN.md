---
phase: 02-backend-integration-testing-code-quality
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified: [backend/coverage.html, backend/coverage.out]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, QUAL-01, QUAL-03, TEST-21]

must_haves:
  truths:
    - "All integration tests pass with explicit assertions"
    - "Code quality tools run without errors"
    - "Coverage baseline is established"
    - "Test execution time is documented"
  artifacts:
    - path: "backend/coverage.out"
      provides: "Coverage data for reporting"
    - path: "backend/coverage.html"
      provides: "Visual coverage report"
  key_links:
    - from: "Makefile"
      to: "test-backend command"
      via: "go test"
      pattern: "go test.*-coverprofile"

---

<objective>
Run complete test suite, establish coverage baseline, and verify all Phase 2 requirements are met.

Purpose: Ensure all tests pass, measure coverage baseline, and confirm Phase 2 success criteria.

Output: Coverage reports, test execution summary, and Phase 2 completion verification.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-integration-testing-code-quality/02-RESEARCH.md

<interfaces>
<!-- Makefile commands -->
From Makefile:
- `make test-backend` - runs tests with coverage
- `make lint` - runs golangci-lint
- `make fmt` - runs go fmt

<!-- Test structure -->
From backend/tests/integration/:
- boundary_auth_test.go
- boundary_collectors_test.go
- boundary_instances_test.go
- boundary_users_test.go
- boundary_validation_test.go
- http_status_codes_test.go (created in 02-02)
- permission_helpers.go (created in 02-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite with coverage</name>
  <files>backend/coverage.out, backend/coverage.html</files>
  <read_first>
    - /Users/glauco.torres/git/pganalytics-v3/Makefile
  </read_first>
  <action>Execute the complete backend test suite with coverage measurement:

1. Run the test suite with coverage:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3/backend
   go test -v -coverprofile=coverage.out ./...
   ```

2. Generate HTML coverage report:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3/backend
   go tool cover -html=coverage.out -o coverage.html
   ```

3. Extract coverage percentage:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3/backend
   go tool cover -func=coverage.out | tail -1
   ```

4. Document the coverage baseline in the summary.

5. Run integration tests separately with timing:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3/backend
   time go test -v ./tests/integration/... -count=1
   ```

Record:
- Total coverage percentage
- Number of tests passed/failed
- Execution time
- Any failing tests and their reasons</action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/backend && go test -v -short ./... 2>&1 | tail -20</automated>
  </verify>
  <done>Test suite runs, coverage.out and coverage.html generated, coverage percentage documented</done>
</task>

<task type="auto">
  <name>Task 2: Verify all code quality checks pass</name>
  <files>backend/**/*.go</files>
  <read_first>
    - /Users/glauco.torres/git/pganalytics-v3/.golangci.yml
  </read_first>
  <action>Run all code quality verification:

1. Run golangci-lint:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3/backend
   golangci-lint run ./...
   ```
   Expected: Exit code 0 (no warnings)

2. Run go vet:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3/backend
   go vet ./...
   ```
   Expected: No issues

3. Run go fmt:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3/backend
   go fmt ./...
   ```
   Expected: No output (already formatted)

4. Run gitleaks secret scan:
   ```bash
   cd /Users/glauco.torres/git/pganalytics-v3
   gitleaks detect --source . --verbose
   ```
   Expected: Exit code 0 (no secrets found)

Document any issues found and whether they were fixed or accepted as exceptions.</action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/backend && golangci-lint run ./... --timeout 5m 2>&1 | tail -30; echo "Exit code: $?"</automated>
  </verify>
  <done>All quality checks pass with zero warnings</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 2 requirements coverage</name>
  <files>.planning/phases/02-backend-integration-testing-code-quality/02-VERIFICATION.md</files>
  <read_first>
    - /Users/glauco.torres/git/pganalytics-v3/.planning/REQUIREMENTS.md
    - /Users/glauco.torres/git/pganalytics-v3/.planning/ROADMAP.md
  </read_first>
  <action>Create a verification document confirming all Phase 2 requirements are met:

Create `.planning/phases/02-backend-integration-testing-code-quality/02-VERIFICATION.md`:

```markdown
# Phase 2 Verification

**Date:** [Current Date]
**Phase:** 02-backend-integration-testing-code-quality

## Requirements Status

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| TEST-01 | API endpoints integration tests | [PASS/FAIL] | Test file names and test counts |
| TEST-02 | Authentication boundary tests | [PASS/FAIL] | Test names in boundary_auth_test.go |
| TEST-03 | Collector endpoints boundary validation | [PASS/FAIL] | Test names in boundary_collectors_test.go |
| TEST-04 | Instance endpoints version/configuration testing | [PASS/FAIL] | Test names in boundary_instances_test.go |
| TEST-05 | User management permission boundaries | [PASS/FAIL] | Test names in boundary_users_test.go |
| TEST-06 | HTTP status codes coverage | [PASS/FAIL] | Test names in http_status_codes_test.go |
| QUAL-01 | Go linting and formatting | [PASS/FAIL] | golangci-lint output |
| QUAL-03 | No hardcoded secrets | [PASS/FAIL] | gitleaks output |
| TEST-21 | Mock/stub configuration | [PASS/FAIL] | README.md in tests/mocks/ |

## Test Results Summary

- **Total Tests:** [N]
- **Passed:** [N]
- **Failed:** [N]
- **Coverage:** [N%]
- **Execution Time:** [N seconds]

## Quality Gate Results

- [ ] golangci-lint: Exit code 0
- [ ] go vet: No issues
- [ ] go fmt: Already formatted
- [ ] gitleaks: No secrets found

## Notes

[Any additional observations, accepted exceptions, or follow-up items]
```

Fill in the actual values from running the tests and quality checks.</action>
  <verify>
    <automated>test -f /Users/glauco.torres/git/pganalytics-v3/.planning/phases/02-backend-integration-testing-code-quality/02-VERIFICATION.md && grep -q "TEST-01" /Users/glauco.torres/git/pganalytics-v3/.planning/phases/02-backend-integration-testing-code-quality/02-VERIFICATION.md</automated>
  </verify>
  <done>Verification document created with all requirement statuses documented</done>
</task>

</tasks>

<verification>
1. Run `make test-backend` - all tests must pass
2. Run `make lint` - exit code 0
3. Run `gitleaks detect --source .` - exit code 0
4. Verify coverage.out exists with coverage data
5. Verify 02-VERIFICATION.md exists with all requirements tracked
</verification>

<success_criteria>
1. All tests in backend/tests/integration/ pass
2. golangci-lint returns exit code 0
3. gitleaks returns exit code 0 (no secrets)
4. Coverage baseline established and documented
5. All 9 Phase 2 requirements verified as complete
6. Verification document exists with requirement status
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration-testing-code-quality/02-06-SUMMARY.md` and update STATE.md
</output>