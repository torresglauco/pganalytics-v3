---
phase: 14-testing-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/replication_test.go
  - backend/tests/integration/host_monitoring_test.go
  - backend/tests/integration/alert_rules_test.go
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "All new API endpoints have integration tests covering happy path and error cases"
    - "Replication API endpoints return correct data structures"
    - "Host monitoring API endpoints handle various status conditions"
    - "Alert rules CRUD operations work end-to-end with database"
    - "Integration tests run against real PostgreSQL test database"
  artifacts:
    - path: "backend/tests/integration/replication_test.go"
      provides: "Replication API integration tests"
      min_lines: 150
      exports: ["TestReplicationAPI"]
    - path: "backend/tests/integration/host_monitoring_test.go"
      provides: "Host monitoring API integration tests"
      min_lines: 150
      exports: ["TestHostMonitoringAPI"]
    - path: "backend/tests/integration/alert_rules_test.go"
      provides: "Alert rules CRUD integration tests"
      min_lines: 200
      exports: ["TestAlertRulesCRUD"]
  key_links:
    - from: "backend/tests/integration/replication_test.go"
      to: "backend/internal/api/handlers_replication.go"
      via: "HTTP request"
      pattern: "/api/v1/collectors/.*/replication"
    - from: "backend/tests/integration/alert_rules_test.go"
      to: "backend/pkg/handlers/alerts.go"
      via: "HTTP request"
      pattern: "/api/v1/alert-rules"
---

<objective>
Create integration tests for backend API endpoints added in Phases 10-12.

Purpose: Verify API endpoints work correctly with real database and full request/response cycle.
Output: Integration test files covering happy path and error scenarios.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-testing-quality/14-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing integration tests -->

From backend/tests/integration/alert_flow_test.go (existing pattern):
```go
func TestAlertFlow_EndToEnd(t *testing.T) {
    if testing.Short() {
        t.Skip("Skipping integration test")
    }
    db := setupTestDatabase(t)
    defer db.Close()
    // Create test data, make HTTP requests, verify responses
}
```

From backend/tests/integration/testhelpers.go:
```go
func setupTestDatabase(t *testing.T) *storage.PostgresDB
func createTestCollector(t *testing.T, db *storage.PostgresDB) *models.Collector
```

From backend/tests/integration/handlers_test.go:
```go
// Pattern: Use httptest for HTTP testing
// Pattern: Test HTTP status codes: 200, 400, 401, 403, 404, 500
// Pattern: Verify JSON response structure with assert.JSON
```

API Endpoints to test:
- GET /api/v1/collectors/:id/replication/topology
- GET /api/v1/collectors/:id/host/status
- GET /api/v1/host-inventory
- POST /api/v1/alert-rules
- GET /api/v1/alert-rules
- PUT /api/v1/alert-rules/:id
- DELETE /api/v1/alert-rules/:id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create replication API integration tests</name>
  <files>
    backend/tests/integration/replication_test.go
  </files>
  <read_first>
    - backend/tests/integration/alert_flow_test.go (for pattern)
    - backend/tests/integration/testhelpers.go
    - backend/internal/api/handlers_replication.go (if exists)
  </read_first>
  <behavior>
    - Test: GET /replication/topology returns 200 with valid topology structure
    - Test: GET /replication/topology returns 404 for non-existent collector
    - Test: GET /replication/topology returns empty topology for standalone instance
    - Test: GET /replication/slots returns list of replication slots
    - Test: GET /replication/status returns streaming replication status
    - Test: Response includes collector_id, node_role, downstream_nodes array
    - Test: Handles unauthorized request with 401
  </behavior>
  <action>
    Create replication_test.go following the alert_flow_test.go pattern. Use setupTestDatabase and createTestCollector helpers. Test the replication topology endpoint. Create test data for primary/standby relationships. Use httptest for making HTTP requests. Verify response JSON structure includes nodes and edges for topology graph. Test both happy path (200) and error cases (404, 401). Add t.Parallel() for concurrent test execution.
  </action>
  <verify>
    <automated>go test ./tests/integration/... -v -run TestReplication -tags=integration</automated>
  </verify>
  <done>
    All 7 test cases pass, topology JSON structure verified, 404 and 401 cases handled
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create host monitoring API integration tests</name>
  <files>
    backend/tests/integration/host_monitoring_test.go
  </files>
  <read_first>
    - backend/tests/integration/alert_flow_test.go (for pattern)
    - backend/tests/integration/testhelpers.go
    - backend/internal/api/handlers_health_score.go
  </read_first>
  <behavior>
    - Test: GET /host/status returns 200 with host status object
    - Test: GET /host/status returns 404 for non-existent collector
    - Test: GET /host-inventory returns list of all hosts with status
    - Test: GET /host-inventory supports filtering by status (up/down)
    - Test: POST /host/metrics stores metrics and triggers health score calculation
    - Test: Health score is calculated and stored after metrics upload
    - Test: GET /host/health/:collectorId returns latest health score
    - Test: Response includes health_score (0-100), status, component scores
    - Test: Handles missing metrics gracefully with zeros
  </behavior>
  <action>
    Create host_monitoring_test.go. Test host status endpoints and health score retrieval. Create test collectors with different health states. Verify health score calculation is triggered after metrics storage. Test filtering in host-inventory endpoint. Use table-driven tests for different host status scenarios. Test boundary conditions for health score (0, 40, 60, 80, 100).
  </action>
  <verify>
    <automated>go test ./tests/integration/... -v -run TestHostMonitoring -tags=integration</automated>
  </verify>
  <done>
    All 9 test cases pass, health score integration verified, filtering works correctly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create alert rules CRUD integration tests</name>
  <files>
    backend/tests/integration/alert_rules_test.go
  </files>
  <read_first>
    - backend/tests/integration/alert_flow_test.go (for pattern)
    - backend/tests/integration/testhelpers.go
    - backend/pkg/handlers/alerts.go
  </read_first>
  <behavior>
    - Test: POST /alert-rules creates rule and returns 201 with rule ID
    - Test: POST /alert-rules returns 400 for invalid rule structure
    - Test: GET /alert-rules returns list of rules for tenant
    - Test: GET /alert-rules/:id returns single rule
    - Test: GET /alert-rules/:id returns 404 for non-existent rule
    - Test: PUT /alert-rules/:id updates rule and returns 200
    - Test: DELETE /alert-rules/:id removes rule and returns 204
    - Test: Alert rule evaluation triggers on metric threshold breach
    - Test: Multi-tenant isolation: user cannot access other tenant's rules
    - Test: Rule with invalid condition returns validation error
    - Test: Enabled/disabled toggle affects rule evaluation
  </behavior>
  <action>
    Create alert_rules_test.go. Test full CRUD lifecycle: Create, Read, Update, Delete. Create test rules with threshold conditions. Test rule evaluation by storing metrics that breach thresholds. Verify multi-tenant isolation by creating rules under different tenants. Use JSON payload for rule creation with fields: name, rule_type, metric_name, condition, severity, is_enabled. Test all HTTP status codes: 201 (created), 200 (ok), 204 (no content), 400 (bad request), 404 (not found), 403 (forbidden).
  </action>
  <verify>
    <automated>go test ./tests/integration/... -v -run TestAlertRules -tags=integration</automated>
  </verify>
  <done>
    All 11 test cases pass, CRUD operations verified, multi-tenant isolation confirmed, evaluation works
  </done>
</task>

</tasks>

<verification>
1. Run all integration tests: `go test ./tests/integration/... -v -tags=integration`
2. Verify tests use test database (not production)
3. Check test coverage: `go test ./tests/integration/... -coverprofile=coverage.out`
4. Ensure tests clean up resources (defer db.Close())
5. All tests must pass with real PostgreSQL connection
</verification>

<success_criteria>
- [ ] backend/tests/integration/replication_test.go exists with 7+ test cases
- [ ] backend/tests/integration/host_monitoring_test.go exists with 9+ test cases
- [ ] backend/tests/integration/alert_rules_test.go exists with 11+ test cases
- [ ] All integration tests pass: `go test ./tests/integration/... -v -tags=integration`
- [ ] Tests use setupTestDatabase helper for test isolation
- [ ] HTTP status codes tested: 200, 201, 204, 400, 401, 403, 404
- [ ] Multi-tenant isolation verified in alert rules tests
</success_criteria>

<output>
After completion, create `.planning/phases/14-testing-quality/14-02-SUMMARY.md`
</output>