---
phase: 04-final-integration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/BulkRuleActions.tsx
  - frontend/src/components/ManagedInstancesTable.tsx
  - frontend/src/components/UserManagementTable.tsx
  - frontend/src/pages/Dashboard.test.tsx
  - frontend/src/components/tables/DataTable.test.tsx
  - frontend/src/components/CollectorList.test.tsx
  - frontend/src/components/CreateUserForm.test.tsx
  - frontend/src/components/LDAPLoginForm.tsx
  - frontend/src/components/LocalLoginForm.tsx
  - frontend/src/components/QueryPlan/PlanTree.tsx
  - frontend/src/components/SignupForm.test.tsx
  - frontend/src/components/alerts/AlertAcknowledgment.test.tsx
  - frontend/src/components/alerts/EscalationPolicyManager.test.tsx
  - frontend/src/components/alerts/EscalationStepEditor.test.tsx
  - frontend/src/components/alerts/SilenceManager.test.tsx
  - frontend/src/pages/NotificationChannelsPage.tsx
autonomous: true
requirements:
  - QUAL-02
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "npm run lint in frontend/ returns exit code 0"
    - "Zero ESLint errors in the codebase"
  artifacts:
    - path: "frontend/src/components/BulkRuleActions.tsx"
      provides: "Bulk rule actions with AlertCircle icon"
      contains: "import.*AlertCircle.*from 'lucide-react'"
    - path: "frontend/src/components/ManagedInstancesTable.tsx"
      provides: "Instances table with AlertCircle icon"
      contains: "import.*AlertCircle.*from 'lucide-react'"
    - path: "frontend/src/components/UserManagementTable.tsx"
      provides: "User management with User icon"
      contains: "import.*User.*from 'lucide-react'"
    - path: "frontend/src/pages/Dashboard.test.tsx"
      provides: "Dashboard tests with proper apiClient mock"
      contains: "const apiClient = vi.importActual"
    - path: "frontend/src/components/tables/DataTable.test.tsx"
      provides: "DataTable tests without unused imports"
      min_lines: 1
    - path: "frontend/src/components/CollectorList.test.tsx"
      provides: "CollectorList tests without unused variables"
      min_lines: 1
    - path: "frontend/src/components/CreateUserForm.test.tsx"
      provides: "CreateUserForm tests without unused imports"
      min_lines: 1
    - path: "frontend/src/components/LDAPLoginForm.tsx"
      provides: "LDAP login without unused variables"
      min_lines: 1
    - path: "frontend/src/components/LocalLoginForm.tsx"
      provides: "Local login without unused variables"
      min_lines: 1
    - path: "frontend/src/components/QueryPlan/PlanTree.tsx"
      provides: "PlanTree without unused parameters"
      min_lines: 1
    - path: "frontend/src/components/SignupForm.test.tsx"
      provides: "SignupForm tests without unused variables"
      min_lines: 1
    - path: "frontend/src/components/alerts/AlertAcknowledgment.test.tsx"
      provides: "AlertAcknowledgment tests without unused imports"
      min_lines: 1
    - path: "frontend/src/components/alerts/EscalationPolicyManager.test.tsx"
      provides: "EscalationPolicyManager tests without unused imports"
      min_lines: 1
    - path: "frontend/src/components/alerts/EscalationStepEditor.test.tsx"
      provides: "EscalationStepEditor tests without unused imports"
      min_lines: 1
    - path: "frontend/src/components/alerts/SilenceManager.test.tsx"
      provides: "SilenceManager tests without unused variables"
      min_lines: 1
    - path: "frontend/src/pages/NotificationChannelsPage.tsx"
      provides: "NotificationChannelsPage without unused variables"
      min_lines: 1
  key_links:
    - from: "BulkRuleActions.tsx"
      to: "lucide-react"
      via: "import"
      pattern: "AlertCircle.*lucide-react"
    - from: "Dashboard.test.tsx"
      to: "services/api"
      via: "vi.mock"
      pattern: "apiClient"
---

<objective>
Fix remaining 26 ESLint errors to achieve exit code 0 for QUAL-02.

Purpose: Complete code quality requirement QUAL-02 by resolving all blocking ESLint errors.
Output: Zero ESLint errors, exit code 0 from `npm run lint` in frontend/.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-final-integration/04-VERIFICATION.md

## Error Categories (26 total)

### Category 1: Missing lucide-react imports (3 errors)
- BulkRuleActions.tsx:103 - AlertCircle not defined
- ManagedInstancesTable.tsx:407 - AlertCircle not defined
- UserManagementTable.tsx:355 - User not defined

### Category 2: Missing apiClient mock/import (7 errors)
- Dashboard.test.tsx lines 67,102,115,126,134,144,153 - apiClient not defined

### Category 3: Unused imports/variables (16 errors in 10 files)
1. CreateUserForm.test.tsx - unused import waitFor
2. LDAPLoginForm.tsx - unused variable _clearErrors
3. LocalLoginForm.tsx - unused variable _clearErrors
4. QueryPlan/PlanTree.tsx - unused parameter index
5. SignupForm.test.tsx - unused variable signupError
6. AlertAcknowledgment.test.tsx - unused imports fireEvent, unused variable rerender
7. EscalationPolicyManager.test.tsx - unused import fireEvent
8. EscalationStepEditor.test.tsx - unused import fireEvent
9. SilenceManager.test.tsx - unused variable rerender
10. NotificationChannelsPage.tsx - unused variable toggleSelectAll
11. DataTable.test.tsx - unused vi, beforeEach, fireEvent
12. CollectorList.test.tsx - unused waitFor, deleteError variable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing lucide-react icon imports</name>
  <files>
    frontend/src/components/BulkRuleActions.tsx,
    frontend/src/components/ManagedInstancesTable.tsx,
    frontend/src/components/UserManagementTable.tsx
  </files>
  <read_first>
    frontend/src/components/BulkRuleActions.tsx (lines 1-5),
    frontend/src/components/ManagedInstancesTable.tsx (lines 1-5),
    frontend/src/components/UserManagementTable.tsx (lines 1-5)
  </read_first>
  <action>
    Add missing icon imports to existing lucide-react import statements:

    1. **BulkRuleActions.tsx line 2:**
       - Current: `import { ChevronDown } from 'lucide-react';`
       - Change to: `import { ChevronDown, AlertCircle } from 'lucide-react';`

    2. **ManagedInstancesTable.tsx line 2:**
       - Current: `import { Trash2, Plus, X, CheckCircle, Edit, Zap } from 'lucide-react'`
       - Change to: `import { Trash2, Plus, X, CheckCircle, Edit, Zap, AlertCircle } from 'lucide-react'`

    3. **UserManagementTable.tsx line 2:**
       - Current: `import { Trash2, Lock, Unlock, Shield, RotateCcw, Copy, CheckCircle, Plus, X } from 'lucide-react'`
       - Change to: `import { Trash2, Lock, Unlock, Shield, RotateCcw, Copy, CheckCircle, Plus, X, User } from 'lucide-react'`

    These components use the icons in JSX (AlertCircle for error/empty states, User for user role toggle button).
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -c "AlertCircle.*is not defined\|User.*is not defined" || echo "0"</automated>
    Expected output: "0" (no missing icon errors)
  </verify>
  <done>
    All three components have the required icon imports added to their lucide-react import statements.
    ESLint no longer reports "AlertCircle is not defined" or "User is not defined" errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix apiClient reference in Dashboard.test.tsx</name>
  <files>
    frontend/src/pages/Dashboard.test.tsx
  </files>
  <read_first>
    frontend/src/pages/Dashboard.test.tsx (lines 1-70)
  </read_first>
  <action>
    The test file mocks apiClient but doesn't import it for use in type assertions.

    Add this import after line 5 (after the User type import):
    ```typescript
    import { apiClient } from '../services/api'
    ```

    The mock at lines 8-12 creates the mock implementation. The import allows the test code to reference `apiClient` for type assertions like `(apiClient.getCurrentUser as ReturnType<typeof vi.fn>).mockReturnValue(...)`.

    Alternative approach (if import causes issues with the mock): Use `vi.importActual` pattern or reference the mock directly through the mocked module. However, the simplest fix is to import the mocked apiClient - Vitest will use the mock implementation due to `vi.mock` being called first.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -c "apiClient.*is not defined" || echo "0"</automated>
    Expected output: "0" (no apiClient undefined errors)
  </verify>
  <done>
    Dashboard.test.tsx properly imports apiClient (which uses the mocked implementation).
    ESLint no longer reports "apiClient is not defined" errors on lines 67, 102, 115, 126, 134, 144, 153.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove all 16 unused imports and variables from 10 files</name>
  <files>
    frontend/src/components/tables/DataTable.test.tsx,
    frontend/src/components/CollectorList.test.tsx,
    frontend/src/components/CreateUserForm.test.tsx,
    frontend/src/components/LDAPLoginForm.tsx,
    frontend/src/components/LocalLoginForm.tsx,
    frontend/src/components/QueryPlan/PlanTree.tsx,
    frontend/src/components/SignupForm.test.tsx,
    frontend/src/components/alerts/AlertAcknowledgment.test.tsx,
    frontend/src/components/alerts/EscalationPolicyManager.test.tsx,
    frontend/src/components/alerts/EscalationStepEditor.test.tsx,
    frontend/src/components/alerts/SilenceManager.test.tsx,
    frontend/src/pages/NotificationChannelsPage.tsx
  </files>
  <read_first>
    frontend/src/components/tables/DataTable.test.tsx (lines 1-5),
    frontend/src/components/CollectorList.test.tsx (lines 1-5, 200-228),
    frontend/src/components/CreateUserForm.test.tsx (lines 1-5),
    frontend/src/components/LDAPLoginForm.tsx (scan for _clearErrors),
    frontend/src/components/LocalLoginForm.tsx (scan for _clearErrors),
    frontend/src/components/QueryPlan/PlanTree.tsx (scan for unused index parameter),
    frontend/src/components/SignupForm.test.tsx (scan for signupError),
    frontend/src/components/alerts/AlertAcknowledgment.test.tsx (lines 1-5),
    frontend/src/components/alerts/EscalationPolicyManager.test.tsx (lines 1-5),
    frontend/src/components/alerts/EscalationStepEditor.test.tsx (lines 1-5),
    frontend/src/components/alerts/SilenceManager.test.tsx (scan for rerender),
    frontend/src/pages/NotificationChannelsPage.tsx (scan for toggleSelectAll)
  </read_first>
  <action>
    Remove all 16 unused imports and variables across 10 files:

    **Test files - unused imports from @testing-library/react:**

    1. **DataTable.test.tsx:**
       - Remove `vi` and `beforeEach` from vitest import (line 1)
       - Remove `fireEvent` from testing-library import (line 2)
       - Final imports: `import { describe, it, expect } from 'vitest';`
       - Final imports: `import { render, screen, waitFor } from '@testing-library/react';`

    2. **CollectorList.test.tsx:**
       - Remove `waitFor` from testing-library import (line 2)
       - Remove unused `deleteError` variable declaration (around line 208)

    3. **CreateUserForm.test.tsx:**
       - Remove `waitFor` from testing-library import

    4. **AlertAcknowledgment.test.tsx:**
       - Remove `fireEvent` from testing-library import
       - Remove unused `rerender` variable from destructuring

    5. **EscalationPolicyManager.test.tsx:**
       - Remove `fireEvent` from testing-library import

    6. **EscalationStepEditor.test.tsx:**
       - Remove `fireEvent` from testing-library import

    7. **SilenceManager.test.tsx:**
       - Remove unused `rerender` variable from destructuring

    **Non-test files - unused variables:**

    8. **LDAPLoginForm.tsx:**
       - Find and remove unused `_clearErrors` variable (likely destructured but never used)
       - Either remove from destructuring or prefix with underscore if intentionally unused

    9. **LocalLoginForm.tsx:**
       - Find and remove unused `_clearErrors` variable
       - Either remove from destructuring or prefix with underscore if intentionally unused

    10. **QueryPlan/PlanTree.tsx:**
        - Find unused `index` parameter (likely in a .map() callback)
        - Either remove the parameter or prefix with underscore: `_index`

    11. **SignupForm.test.tsx:**
        - Find and remove unused `signupError` variable

    12. **NotificationChannelsPage.tsx:**
        - Find and remove unused `toggleSelectAll` variable

    General approach for each file:
    - Read the file to locate the unused import/variable
    - Remove it from the import statement or variable declaration
    - For callback parameters like `index`, use underscore prefix `_index` if removal breaks syntax
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | grep -E "'(vi|beforeEach|fireEvent|waitFor|deleteError|_clearErrors|index|signupError|rerender|toggleSelectAll)' is defined but never used" | wc -l | tr -d ' '</automated>
    Expected output: "0" (no unused variable errors for these identifiers)
  </verify>
  <done>
    All 10 files have been cleaned of unused imports and variables.
    ESLint no longer reports any of the 16 unused-vars errors.
    DataTable.test.tsx, CollectorList.test.tsx, CreateUserForm.test.tsx, and all alert test files have minimal required imports.
    LDAPLoginForm.tsx, LocalLoginForm.tsx, PlanTree.tsx, SignupForm.test.tsx, and NotificationChannelsPage.tsx have no unused variable declarations.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify ESLint passes with exit code 0</name>
  <files>
    None (verification only)
  </files>
  <read_first>
    None
  </read_first>
  <action>
    Run the full ESLint check and verify exit code 0:

    ```bash
    cd frontend && npm run lint
    ```

    The expected result is:
    - Exit code: 0 (success)
    - Errors: 0
    - Warnings: ~161 (no-explicit-any warnings are acceptable - not blocking)

    If any errors remain, analyze the output and identify what additional fixes are needed.
  </action>
  <verify>
    <automated>cd frontend && npm run lint; echo "EXIT_CODE=$?"</automated>
    Expected output: Exit code 0, zero errors reported
  </verify>
  <done>
    `npm run lint` in frontend/ returns exit code 0.
    QUAL-02 requirement is fully satisfied.
    The codebase has zero ESLint errors (warnings are acceptable).
  </done>
</task>

</tasks>

<verification>
Run the following to verify all gaps are closed:

```bash
# Verify ESLint passes with exit code 0
cd frontend && npm run lint

# Verify zero errors (warnings are OK)
cd frontend && npm run lint 2>&1 | grep -E "✖ [0-9]+ errors" || echo "No errors found"

# Verify specific files are fixed
cd frontend && npm run lint -- src/components/BulkRuleActions.tsx src/components/ManagedInstancesTable.tsx src/components/UserManagementTable.tsx src/pages/Dashboard.test.tsx src/components/tables/DataTable.test.tsx src/components/CollectorList.test.tsx src/components/CreateUserForm.test.tsx src/components/LDAPLoginForm.tsx src/components/LocalLoginForm.tsx src/components/QueryPlan/PlanTree.tsx src/components/SignupForm.test.tsx src/components/alerts/AlertAcknowledgment.test.tsx src/components/alerts/EscalationPolicyManager.test.tsx src/components/alerts/EscalationStepEditor.test.tsx src/components/alerts/SilenceManager.test.tsx src/pages/NotificationChannelsPage.tsx
```
</verification>

<success_criteria>
QUAL-02 requirement fully satisfied:
1. `npm run lint` in frontend/ returns exit code 0
2. Zero ESLint errors in the codebase
3. All 26 errors from VERIFICATION.md resolved:
   - 3 missing lucide-react imports added
   - 7 apiClient references fixed
   - 16 unused imports/variables removed across 10 files
</success_criteria>

<output>
After completion, create `.planning/phases/04-final-integration/04-07-SUMMARY.md`
</output>