---
phase: 14-testing-quality
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/components/topology/TopologyGraph.test.tsx
  - frontend/src/components/classification/ClassificationTable.test.tsx
  - frontend/src/components/host/HostStatusTable.test.tsx
  - frontend/src/pages/ReplicationTopologyPage.test.tsx
  - frontend/src/pages/DataClassificationPage.test.tsx
  - frontend/src/pages/HostInventoryPage.test.tsx
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "All new frontend components have tests passing"
    - "Topology graph renders nodes and edges correctly"
    - "Classification table displays PII/PCI data with filters"
    - "Host status table shows up/down status correctly"
    - "Pages handle loading, error, and empty states"
  artifacts:
    - path: "frontend/src/components/topology/TopologyGraph.test.tsx"
      provides: "Topology graph component tests"
      min_lines: 100
    - path: "frontend/src/components/classification/ClassificationTable.test.tsx"
      provides: "Classification table component tests"
      min_lines: 80
    - path: "frontend/src/components/host/HostStatusTable.test.tsx"
      provides: "Host status table component tests"
      min_lines: 80
    - path: "frontend/src/pages/ReplicationTopologyPage.test.tsx"
      provides: "Replication topology page tests"
      min_lines: 120
    - path: "frontend/src/pages/DataClassificationPage.test.tsx"
      provides: "Data classification page tests"
      min_lines: 120
    - path: "frontend/src/pages/HostInventoryPage.test.tsx"
      provides: "Host inventory page tests"
      min_lines: 120
  key_links:
    - from: "frontend/src/pages/ReplicationTopologyPage.test.tsx"
      to: "frontend/src/api/replicationApi.ts"
      via: "vi.mock"
      pattern: "getTopology"
    - from: "frontend/src/pages/DataClassificationPage.test.tsx"
      to: "frontend/src/api/classificationApi.ts"
      via: "vi.mock"
      pattern: "getClassificationResults"
    - from: "frontend/src/pages/HostInventoryPage.test.tsx"
      to: "frontend/src/api/hostApi.ts"
      via: "vi.mock"
      pattern: "hostApi.getAllHostStatuses"
---

<objective>
Create comprehensive unit tests for frontend components and pages added in Phase 13.

Purpose: Ensure UI components render correctly and handle user interactions as expected.
Output: Vitest test files with @testing-library/react for all new components.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-testing-quality/14-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing frontend tests -->

From frontend/src/App.test.tsx (existing pattern):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

vi.mock('./services/api')

describe('Component', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('should render correctly', async () => {
    vi.mocked(api.getData).mockResolvedValue(mockData)
    render(<Component />)
    await waitFor(() => {
      expect(screen.getByText('Expected Text')).toBeInTheDocument()
    })
  })
})
```

From frontend/src/pages/Dashboard.test.tsx:
```typescript
// Pattern: Test loading state, error state, empty state, data rendering
// Pattern: Mock API calls with vi.mock
// Pattern: Use getByRole, getByText, getByTestId for queries
// Pattern: Use userEvent for interactions
```

Key types from frontend/src/types/:
```typescript
interface ReplicationTopology {
  collector_id: string
  node_role: 'primary' | 'standby'
  downstream_nodes: DownstreamNode[]
}

interface DataClassificationResult {
  database_name: string
  schema_name: string
  table_name: string
  pattern_type: PatternType
  category: Category
}

interface HostStatus {
  collector_id: string
  hostname: string
  status: 'up' | 'down' | 'unknown'
  is_healthy: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TopologyGraph component tests</name>
  <files>
    frontend/src/components/topology/TopologyGraph.test.tsx
  </files>
  <read_first>
    - frontend/src/components/topology/TopologyGraph.tsx
    - frontend/src/types/replication.ts
    - frontend/src/App.test.tsx (for pattern)
  </read_first>
  <behavior>
    - Test: Renders primary node with correct label
    - Test: Renders standby nodes below primary
    - Test: Renders edges connecting nodes
    - Test: Displays lag metrics on node hover
    - Test: Shows correct node colors (green=primary, blue=standby)
    - Test: Handles empty topology gracefully
    - Test: MiniMap shows correct node colors
  </behavior>
  <action>
    Create TopologyGraph.test.tsx with Vitest and @testing-library/react. Mock the ReactFlow component or use real rendering. Test with mock topology data containing primary and standby nodes. Verify node rendering by checking for role labels. Test edge rendering by verifying connections exist. Use queryByRole and getByText for assertions. Follow the existing App.test.tsx pattern for setup and mocking.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/frontend && npm run test -- --filter=TopologyGraph</automated>
  </verify>
  <done>
    All 7 test cases pass, topology rendering verified, lag metrics displayed correctly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ClassificationTable component tests</name>
  <files>
    frontend/src/components/classification/ClassificationTable.test.tsx
  </files>
  <read_first>
    - frontend/src/components/classification/ClassificationTable.tsx
    - frontend/src/types/classification.ts
    - frontend/src/App.test.tsx (for pattern)
  </read_first>
  <behavior>
    - Test: Renders table with columns: Database, Schema, Table, Pattern, Category
    - Test: Displays PII pattern type badge
    - Test: Displays PCI pattern type badge with different color
    - Test: Shows sample count for each classification
    - Test: Row click triggers onRowClick callback
    - Test: Loading state shows spinner
    - Test: Empty state shows "No data" message
  </behavior>
  <action>
    Create ClassificationTable.test.tsx. Test the classification table component with mock data. Verify table columns render correctly. Test pattern type badges (CPF, CNPJ, EMAIL, CREDIT_CARD). Test category badges (PII, PCI, SENSITIVE). Use userEvent.click for row interactions. Test loading and empty states.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/frontend && npm run test -- --filter=ClassificationTable</automated>
  </verify>
  <done>
    All 7 test cases pass, table columns verified, badges colored correctly, interactions work
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create HostStatusTable component tests</name>
  <files>
    frontend/src/components/host/HostStatusTable.test.tsx
  </files>
  <read_first>
    - frontend/src/components/host/HostStatusTable.tsx
    - frontend/src/types/host.ts
    - frontend/src/App.test.tsx (for pattern)
  </read_first>
  <behavior>
    - Test: Renders table with columns: Hostname, Status, Health, Last Seen
    - Test: Shows "up" status with green indicator
    - Test: Shows "down" status with red indicator
    - Test: Shows "unknown" status with gray indicator
    - Test: Displays health score badge (healthy/degraded/warning/critical)
    - Test: Row click triggers onSelectHost callback
    - Test: Selected row is highlighted
    - Test: Loading state shows spinner
  </behavior>
  <action>
    Create HostStatusTable.test.tsx. Test the host status table with mock host data. Verify status indicators render with correct colors. Test health score badges match score ranges. Test row selection interaction. Use getByRole('row') for table row queries. Test loading state displays spinner.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/frontend && npm run test -- --filter=HostStatusTable</automated>
  </verify>
  <done>
    All 8 test cases pass, status colors correct, health badges verified, selection works
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create ReplicationTopologyPage tests</name>
  <files>
    frontend/src/pages/ReplicationTopologyPage.test.tsx
  </files>
  <read_first>
    - frontend/src/pages/ReplicationTopologyPage.tsx
    - frontend/src/api/replicationApi.ts
    - frontend/src/pages/Dashboard.test.tsx (for pattern)
  </read_first>
  <behavior>
    - Test: Shows loading spinner initially
    - Test: Renders topology graph after data loads
    - Test: Displays error message on API failure
    - Test: Shows empty state when no topology data
    - Test: Refresh button triggers data reload
    - Test: Handles missing collectorId in URL params
    - Test: Topology legend renders in sidebar
  </behavior>
  <action>
    Create ReplicationTopologyPage.test.tsx. Mock getTopology from replicationApi. Test all page states: loading, error, empty, data. Use react-router-dom's MemoryRouter for URL params. Test refresh button with userEvent.click. Verify error message display on API failure. Test that TopologyGraph receives correct props after data load.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/frontend && npm run test -- --filter=ReplicationTopologyPage</automated>
  </verify>
  <done>
    All 7 test cases pass, loading/error/empty states verified, refresh works, legend renders
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create DataClassificationPage tests</name>
  <files>
    frontend/src/pages/DataClassificationPage.test.tsx
  </files>
  <read_first>
    - frontend/src/pages/DataClassificationPage.tsx
    - frontend/src/api/classificationApi.ts
    - frontend/src/pages/Dashboard.test.tsx (for pattern)
  </read_first>
  <behavior>
    - Test: Shows loading spinner initially
    - Test: Renders classification table after data loads
    - Test: Displays summary cards with pattern counts
    - Test: Filters update when dropdown changes
    - Test: Breadcrumb navigation works for drill-down
    - Test: Error message displays on API failure
    - Test: Export button is visible (functionality tested separately)
    - Test: Pattern breakdown chart renders with data
  </behavior>
  <action>
    Create DataClassificationPage.test.tsx. Mock getClassificationResults and getClassificationReport. Test the full page flow with filter state. Test breadcrumb click resets filters appropriately. Use MemoryRouter for URL parameter testing. Verify summary cards display correct counts from report data. Test filter dropdowns change visible data.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/frontend && npm run test -- --filter=DataClassificationPage</automated>
  </verify>
  <done>
    All 8 test cases pass, filters work, drill-down verified, summary cards correct
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create HostInventoryPage tests</name>
  <files>
    frontend/src/pages/HostInventoryPage.test.tsx
  </files>
  <read_first>
    - frontend/src/pages/HostInventoryPage.tsx
    - frontend/src/api/hostApi.ts
    - frontend/src/pages/Dashboard.test.tsx (for pattern)
  </read_first>
  <behavior>
    - Test: Shows loading spinner initially
    - Test: Renders host table after data loads
    - Test: Displays summary cards with up/down counts
    - Test: Search filters hosts by hostname
    - Test: Status filter dropdown filters correctly
    - Test: Error message displays on API failure
    - Test: Auto-refresh toggle is visible
    - Test: Export button generates CSV download
    - Test: Clicking host opens detail panel
  </behavior>
  <action>
    Create HostInventoryPage.test.tsx. Mock hostApi.getAllHostStatuses. Test search and filter functionality. Test status filter dropdown. Test that clicking a host triggers detail panel. Verify summary cards show correct counts. Test auto-refresh checkbox is present. Test export functionality triggers download.
  </action>
  <verify>
    <automated>cd /Users/glauco.torres/git/pganalytics-v3/frontend && npm run test -- --filter=HostInventoryPage</automated>
  </verify>
  <done>
    All 9 test cases pass, search/filter verified, detail panel opens, export works
  </done>
</task>

</tasks>

<verification>
1. Run all frontend tests: `cd frontend && npm run test`
2. Run with coverage: `cd frontend && npm run test:coverage`
3. Verify no test failures
4. Check coverage report shows 80%+ on new components
5. All tests must pass in CI environment
</verification>

<success_criteria>
- [ ] frontend/src/components/topology/TopologyGraph.test.tsx exists with 7+ test cases
- [ ] frontend/src/components/classification/ClassificationTable.test.tsx exists with 7+ test cases
- [ ] frontend/src/components/host/HostStatusTable.test.tsx exists with 8+ test cases
- [ ] frontend/src/pages/ReplicationTopologyPage.test.tsx exists with 7+ test cases
- [ ] frontend/src/pages/DataClassificationPage.test.tsx exists with 8+ test cases
- [ ] frontend/src/pages/HostInventoryPage.test.tsx exists with 9+ test cases
- [ ] All tests pass: `cd frontend && npm run test`
- [ ] Coverage >= 80% on new components
</success_criteria>

<output>
After completion, create `.planning/phases/14-testing-quality/14-03-SUMMARY.md`
</output>