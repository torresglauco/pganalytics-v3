---
phase: 11-data-classification-health-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pkg/models/classification_models.go
  - backend/migrations/032_data_classification_tables.sql
  - backend/internal/storage/classification_store.go
  - backend/internal/api/handlers_data_classification.go
  - backend/internal/api/server.go
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
must_haves:
  truths:
    - "User can view PII detection results for sensitive data patterns (CPF, CNPJ, email, phone)"
    - "User can view PCI detection results for credit card numbers"
    - "User can view LGPD/GDPR regulated data identification"
    - "User can configure custom detection patterns"
    - "User can view data classification reports by database/table"
  artifacts:
    - path: "backend/pkg/models/classification_models.go"
      provides: "Data structures for classification results"
      min_lines: 50
    - path: "backend/migrations/032_data_classification_tables.sql"
      provides: "TimescaleDB tables for classification storage"
      contains: "CREATE TABLE.*metrics_data_classification"
    - path: "backend/internal/storage/classification_store.go"
      provides: "Database operations for classification"
      exports: ["StoreClassificationResults", "GetClassificationResults"]
    - path: "backend/internal/api/handlers_data_classification.go"
      provides: "HTTP handlers for classification endpoints"
      exports: ["handleGetClassificationResults", "handleGetCustomPatterns"]
  key_links:
    - from: "backend/internal/api/handlers_data_classification.go"
      to: "backend/internal/storage/classification_store.go"
      via: "storage.PostgresDB methods"
      pattern: "p\\.store\\."
---

<objective>
Implement PII/PCI data classification backend with models, database schema, storage layer, and REST API endpoints for viewing detection results and managing custom patterns.

Purpose: Enable users to identify sensitive data in their PostgreSQL databases through automated pattern detection, supporting compliance with LGPD/GDPR regulations.
Output: Complete backend for data classification with TimescaleDB storage and REST API.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-data-classification-health-analysis/11-RESEARCH.md

<interfaces>
<!-- Key patterns from Phase 10 for executor reference -->

From backend/pkg/models/replication_models.go:
```go
type ReplicationStatus struct {
    Time          time.Time `json:"time" db:"time"`
    CollectorID   uuid.UUID `json:"collector_id" db:"collector_id"`
    // ... fields with db and json tags
}
```

From backend/internal/storage/host_store.go:
```go
func (p *PostgresDB) StoreHostMetrics(ctx context.Context, metrics *models.HostMetrics) error {
    // Pattern: prepared statement with TimescaleDB insert
}
```

From backend/internal/api/handlers_host.go:
```go
// @Summary Get host status
// @Tags hosts
// @Produce json
// @Success 200 {object} models.HostStatusResponse
func (s *Server) handleGetHostStatus(c *gin.Context) { }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create data classification models</name>
  <files>backend/pkg/models/classification_models.go</files>
  <read_first>
    - backend/pkg/models/host_models.go (pattern for model structure)
    - backend/pkg/models/inventory_models.go (pattern for inventory-style models)
  </read_first>
  <behavior>
    - Test: DataClassificationResult struct has all required fields (time, collector_id, database_name, schema_name, table_name, column_name, pattern_type, category, confidence, match_count)
    - Test: CustomPattern struct supports tenant_id for tenant-specific patterns
    - Test: JSON tags match database column names for serialization
  </behavior>
  <action>Create classification_models.go with the following structs:

1. DataClassificationResult struct with fields:
   - Time (time.Time, db:"time", json:"time")
   - CollectorID (uuid.UUID, db:"collector_id", json:"collector_id")
   - DatabaseName (string, db:"database_name", json:"database_name")
   - SchemaName (string, db:"schema_name", json:"schema_name")
   - TableName (string, db:"table_name", json:"table_name")
   - ColumnName (string, db:"column_name", json:"column_name")
   - PatternType (string, db:"pattern_type", json:"pattern_type") - values: CPF, CNPJ, EMAIL, PHONE, CREDIT_CARD, CUSTOM
   - Category (string, db:"category", json:"category") - values: PII, PCI, SENSITIVE, CUSTOM
   - Confidence (float64, db:"confidence", json:"confidence") - 0.0 to 1.0
   - MatchCount (int64, db:"match_count", json:"match_count")
   - SampleValues ([]string, db:"sample_values", json:"sample_values") - masked samples
   - RegulationMapping (map[string][]string, db:"regulation_mapping", json:"regulation_mapping") - LGPD/GDPR article references

2. CustomPattern struct with fields:
   - ID (int, db:"id", json:"id")
   - TenantID (uuid.NullUUID, db:"tenant_id", json:"tenant_id") - NULL for global patterns
   - PatternName (string, db:"pattern_name", json:"pattern_name")
   - PatternRegex (string, db:"pattern_regex", json:"pattern_regex")
   - Category (string, db:"category", json:"category")
   - ValidationAlgorithm (string, db:"validation_algorithm", json:"validation_algorithm") - Luhn, Mod11, None
   - Description (string, db:"description", json:"description")
   - Enabled (bool, db:"enabled", json:"enabled")

3. ClassificationReportResponse struct for aggregated results

Use exact struct tags pattern from host_models.go: `db:"column_name" json:"json_name"`.</action>
  <verify>
    <automated>go build ./backend/pkg/models</automated>
  </verify>
  <done>
    - classification_models.go exists with DataClassificationResult, CustomPattern, ClassificationReportResponse structs
    - All structs have db and json tags matching column names
    - Package compiles successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Create data classification database migration</name>
  <files>backend/migrations/032_data_classification_tables.sql</files>
  <read_first>
    - backend/migrations/031_replication_tables.sql (pattern for TimescaleDB hypertables)
    - backend/pkg/models/classification_models.go (created in Task 1)
  </read_first>
  <behavior>
    - Test: metrics_data_classification table has all columns from DataClassificationResult model
    - Test: data_classification_patterns table supports tenant_id for multi-tenancy
    - Test: TimescaleDB hypertable created with create_hypertable call
  </behavior>
  <action>Create migration 032_data_classification_tables.sql with:

1. metrics_data_classification table (DATA-01, DATA-02, DATA-03):
   - time TIMESTAMPTZ NOT NULL
   - collector_id UUID NOT NULL REFERENCES collectors(id)
   - database_name, schema_name, table_name, column_name VARCHAR(255)
   - pattern_type VARCHAR(50) - CPF, CNPJ, EMAIL, PHONE, CREDIT_CARD, CUSTOM
   - category VARCHAR(20) - PII, PCI, SENSITIVE, CUSTOM
   - confidence FLOAT
   - match_count BIGINT
   - sample_values JSONB
   - regulation_mapping JSONB
   - PRIMARY KEY (time, collector_id, database_name, schema_name, table_name, column_name)
   - TimescaleDB hypertable with 90-day retention
   - Index on (collector_id, time DESC)
   - Index on (pattern_type, category)

2. data_classification_patterns table (DATA-04):
   - id SERIAL PRIMARY KEY
   - tenant_id UUID (NULL for global patterns)
   - pattern_name VARCHAR(255) NOT NULL
   - pattern_regex TEXT NOT NULL
   - category VARCHAR(20) NOT NULL
   - validation_algorithm VARCHAR(50)
   - description TEXT
   - created_at, updated_at TIMESTAMPTZ
   - enabled BOOLEAN DEFAULT TRUE

3. Seed regulation mappings:
   - CPF: {"LGPD": ["Art. 5, I - dado pessoal"]}
   - CNPJ: {"LGPD": ["Art. 5, I - dado pessoal"]}
   - CREDIT_CARD: {"PCI-DSS": ["Req. 3"], "LGPD": ["Art. 5, I"]}

Use BEGIN/COMMIT transaction wrapping like 031_replication_tables.sql.</action>
  <verify>
    <automated>grep -c "metrics_data_classification" backend/migrations/032_data_classification_tables.sql && grep -c "create_hypertable" backend/migrations/032_data_classification_tables.sql</automated>
  </verify>
  <done>
    - Migration file exists with metrics_data_classification table
    - Migration file exists with data_classification_patterns table
    - TimescaleDB hypertable configured
    - Indexes created for query performance
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create classification store with prepared statements</name>
  <files>backend/internal/storage/classification_store.go</files>
  <read_first>
    - backend/internal/storage/host_store.go (pattern for store methods)
    - backend/internal/storage/inventory_store.go (pattern for inventory storage)
    - backend/pkg/models/classification_models.go (models from Task 1)
  </read_first>
  <behavior>
    - Test: StoreClassificationResults inserts batch of classification results
    - Test: GetClassificationResults filters by collector_id, database, table
    - Test: StoreCustomPattern validates pattern_regex
  </behavior>
  <action>Create classification_store.go with the following methods on PostgresDB:

1. StoreClassificationResults(ctx context.Context, results []*models.DataClassificationResult) error
   - Use prepared statement with INSERT ... ON CONFLICT DO UPDATE
   - Batch insert using transaction
   - Handle regulation_mapping as JSONB

2. GetClassificationResults(ctx context.Context, collectorID uuid.UUID, filters ClassificationFilter) ([]*models.DataClassificationResult, error)
   - Filters: database_name, schema_name, table_name, pattern_type, category
   - Support time_range: 1h, 24h, 7d, 30d
   - Order by time DESC

3. GetClassificationReport(ctx context.Context, collectorID uuid.UUID) (*models.ClassificationReportResponse, error)
   - Aggregate by database, schema, table
   - Count distinct pattern types per table
   - Sum match_count per category

4. GetCustomPatterns(ctx context.Context, tenantID *uuid.UUID) ([]*models.CustomPattern, error)
   - Return global patterns (tenant_id IS NULL) + tenant-specific patterns

5. CreateCustomPattern(ctx context.Context, pattern *models.CustomPattern) error
   - Validate pattern_regex is valid regex
   - Insert with created_at/updated_at timestamps

6. UpdateCustomPattern(ctx context.Context, id int, pattern *models.CustomPattern) error
   - Update pattern_regex, category, enabled, updated_at

7. DeleteCustomPattern(ctx context.Context, id int) error

Follow the prepared statement pattern from host_store.go with proper error handling using apperrors package.</action>
  <verify>
    <automated>go build ./backend/internal/storage</automated>
  </verify>
  <done>
    - classification_store.go exists with 7 methods
    - StoreClassificationResults handles batch insert
    - GetClassificationResults supports filtering and pagination
    - Package compiles successfully
  </done>
</task>

<task type="auto">
  <name>Task 4: Create data classification API handlers</name>
  <files>backend/internal/api/handlers_data_classification.go</files>
  <read_first>
    - backend/internal/api/handlers_host.go (pattern for API handlers)
    - backend/internal/api/handlers_inventory.go (pattern for inventory handlers)
    - backend/internal/storage/classification_store.go (store from Task 3)
  </read_first>
  <behavior>
    - Test: GET /api/v1/collectors/:id/classification returns 200 with results array
    - Test: GET /api/v1/classification/patterns returns global and tenant patterns
    - Test: POST /api/v1/classification/patterns validates regex pattern
  </behavior>
  <action>Create handlers_data_classification.go with the following handlers:

1. handleGetClassificationResults(c *gin.Context)
   - Route: GET /api/v1/collectors/:id/classification
   - Parse collector_id from URL param
   - Parse query params: database, schema, table, pattern_type, category, time_range
   - Call store.GetClassificationResults
   - Return JSON with 200 status
   - Swagger annotation: @Summary Get data classification results

2. handleGetClassificationReport(c *gin.Context)
   - Route: GET /api/v1/collectors/:id/classification/report
   - Return aggregated report by database/table
   - Swagger annotation: @Summary Get classification report

3. handleGetCustomPatterns(c *gin.Context)
   - Route: GET /api/v1/classification/patterns
   - Get tenant_id from context (from auth middleware)
   - Call store.GetCustomPatterns
   - Return array of patterns

4. handleCreateCustomPattern(c *gin.Context)
   - Route: POST /api/v1/classification/patterns
   - Bind JSON body to CustomPattern
   - Validate pattern_regex is valid regex using regexp.Compile
   - Call store.CreateCustomPattern
   - Return 201 with created pattern

5. handleUpdateCustomPattern(c *gin.Context)
   - Route: PUT /api/v1/classification/patterns/:id
   - Bind JSON body, validate, update

6. handleDeleteCustomPattern(c *gin.Context)
   - Route: DELETE /api/v1/classification/patterns/:id
   - Call store.DeleteCustomPattern
   - Return 204

Use the same pattern as handlers_host.go: parse params, call store, return JSON. Add proper error handling with c.JSON(400/404/500, gin.H{"error": ...}).</action>
  <verify>
    <automated>go build ./backend/internal/api</automated>
  </verify>
  <done>
    - handlers_data_classification.go exists with 6 handlers
    - All handlers have Swagger annotations
    - Error handling returns appropriate HTTP status codes
    - Package compiles successfully
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire classification routes in server</name>
  <files>backend/internal/api/server.go</files>
  <read_first>
    - backend/internal/api/server.go (current route registration)
    - backend/internal/api/handlers_data_classification.go (handlers from Task 4)
  </read_first>
  <behavior>
    - Test: GET /api/v1/collectors/:id/classification route returns 401 without auth
    - Test: Route is registered with auth middleware
  </behavior>
  <action>Add the following routes to server.go in the setupRoutes() function, under the collectors group:

```go
// Data classification routes
collectors.GET("/:id/classification", s.handleGetClassificationResults)
collectors.GET("/:id/classification/report", s.handleGetClassificationReport)

// Custom patterns (admin only - requires auth)
api.GET("/classification/patterns", s.AuthMiddleware(), s.handleGetCustomPatterns)
api.POST("/classification/patterns", s.AuthMiddleware(), s.handleCreateCustomPattern)
api.PUT("/classification/patterns/:id", s.AuthMiddleware(), s.handleUpdateCustomPattern)
api.DELETE("/classification/patterns/:id", s.AuthMiddleware(), s.handleDeleteCustomPattern)
```

Place these after the existing inventory routes (after line with inventory routes). Use the same authentication pattern as existing routes.</action>
  <verify>
    <automated>grep -c "classification" backend/internal/api/server.go</automated>
  </verify>
  <done>
    - 6 classification routes registered in server.go
    - Routes use AuthMiddleware where appropriate
    - go build ./backend/internal/api passes
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Backend compiles: `go build ./backend/...`
2. Classification models exist with proper tags
3. Migration creates TimescaleDB tables
4. Store methods work with prepared statements
5. API handlers return proper responses
6. Routes registered with authentication
</verification>

<success_criteria>
1. User can view PII detection results (CPF, CNPJ, email, phone) via GET /api/v1/collectors/:id/classification
2. User can view PCI detection results (credit cards) in same endpoint
3. User can view LGPD/GDPR regulation mapping in classification results
4. User can configure custom detection patterns via POST /api/v1/classification/patterns
5. User can view classification reports by database/table via GET /api/v1/collectors/:id/classification/report
</success_criteria>

<output>
After completion, create `.planning/phases/11-data-classification-health-analysis/11-01-SUMMARY.md`
</output>