---
phase: 10-collector-backend-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - backend/pkg/models/inventory_models.go
  - backend/internal/storage/inventory_store.go
  - backend/internal/api/handlers_inventory.go
  - backend/migrations/031_replication_tables.sql
  - backend/internal/api/server.go
  - collector/src/schema_plugin.cpp
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - INV-05
must_haves:
  truths:
    - "User can view table inventory with row counts and sizes from pg_stat_user_tables"
    - "User can view column inventory with data types and nullability from information_schema"
    - "User can view index inventory with usage statistics from pg_stat_user_indexes"
    - "User can view extension inventory with versions from pg_extension"
    - "User can track schema changes over time via version tracking"
  artifacts:
    - path: "backend/pkg/models/inventory_models.go"
      provides: "Data structures for database inventory"
      exports: ["TableInventory", "ColumnInventory", "IndexInventory", "ExtensionInventory", "SchemaVersion"]
    - path: "backend/internal/storage/inventory_store.go"
      provides: "Database operations for inventory"
      exports: ["StoreTableInventory", "GetTableInventory", "StoreColumnInventory", "StoreIndexInventory", "TrackSchemaChanges"]
    - path: "backend/internal/api/handlers_inventory.go"
      provides: "HTTP handlers for inventory endpoints"
      exports: ["handleGetTableInventory", "handleGetColumnInventory", "handleGetIndexInventory"]
    - path: "collector/src/schema_plugin.cpp"
      provides: "Enhanced schema collection with sizes"
      contains: "pg_total_relation_size"
  key_links:
    - from: "backend/internal/api/handlers_inventory.go"
      to: "backend/internal/storage/inventory_store.go"
      via: "s.postgres.GetTableInventory"
    - from: "collector/src/schema_plugin.cpp"
      to: "pg_stat_user_tables"
      via: "SELECT for table sizes and row counts"
---

<objective>
Implement database inventory backend for tables, columns, indexes, and extensions with schema change tracking. This enables users to track database objects and detect schema changes over time.

Purpose: Provide comprehensive database inventory for asset tracking, capacity planning, and change management.
Output: Working API endpoints for inventory with enhanced C++ collector that includes table sizes.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From collector/src/schema_plugin.cpp (already collecting):
```cpp
json PgSchemaCollector::collectTableSchema(const std::string& dbname) {
    // Query information_schema.tables
}

json PgSchemaCollector::collectIndexInfo(const std::string& dbname) {
    // Query pg_stat_user_indexes for index stats
}
```

From backend/internal/storage/metrics_store.go:
```go
func (p *PostgresDB) StoreSchemaMetrics(ctx context.Context, tables []*models.SchemaTable, ...) error {
    // Insert into metrics_pg_schema_tables
}
```

Enhancement needed for INV-01 (table sizes):
- Add pg_total_relation_size(relid) to table collection
- Add n_live_tup from pg_stat_user_tables for row counts

Enhancement for INV-03 (index usage):
- Already collecting idx_scan, idx_tup_read, idx_tup_fetch
- Add index size via pg_relation_size(indexrelid)

Enhancement for INV-05 (schema changes):
- Compare current snapshot with previous
- Detect added/removed/modified tables, columns, indexes
- Store changes in metrics_schema_versions table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create inventory models</name>
  <files>
    backend/pkg/models/inventory_models.go
  </files>
  <read_first>
    - backend/pkg/models/models.go (model patterns)
    - backend/pkg/models/metrics_models.go (response pattern)
    - collector/src/schema_plugin.cpp (fields being collected)
  </read_first>
  <action>
    Create backend/pkg/models/inventory_models.go:

    1. TableInventory struct for INV-01:
       - Time time.Time, CollectorID uuid.UUID, DatabaseName string
       - SchemaName string, TableName string, TableType string (BASE TABLE, VIEW)
       - RowCount int64 (from n_live_tup)
       - TotalSizeMb int64 (pg_total_relation_size / 1024 / 1024)
       - TableSizeMb int64 (pg_relation_size / 1024 / 1024)
       - IndexSizeMb int64 (index size only)
       - ToastSizeMb int64 (TOAST size)
       - HasOids bool
       - TableOid uint64 (for change detection)

    2. ColumnInventory struct for INV-02:
       - Time time.Time, CollectorID uuid.UUID, DatabaseName string
       - SchemaName string, TableName string, ColumnName string
       - DataType string, IsNullable bool, ColumnDefault string
       - OrdinalPosition int
       - CharacterMaxLength int, NumericPrecision int, NumericScale int
       - IsPrimaryKey bool, IsForeignKey bool

    3. IndexInventory struct for INV-03:
       - Time time.Time, CollectorID uuid.UUID, DatabaseName string
       - SchemaName string, TableName string, IndexName string
       - IndexDefinition string (from pg_get_indexdef)
       - IndexSizeMb int64 (pg_relation_size / 1024 / 1024)
       - IdxScan int64, IdxTupRead int64, IdxTupFetch int64
       - UsageStatus string (UNUSED if idx_scan=0, RARELY_USED if <100, ACTIVE otherwise)
       - IsPrimary bool, IsUnique bool
       - IndexOid uint64

    4. ExtensionInventory struct for INV-04:
       - Time time.Time, CollectorID uuid.UUID, DatabaseName string
       - ExtensionName string, ExtensionVersion string
       - ExtensionOwner string, ExtensionSchema string
       - IsRelocatable bool
       - Description string

    5. SchemaVersion struct for INV-05:
       - Time time.Time, CollectorID uuid.UUID, DatabaseName string
       - VersionHash string (MD5 of all object signatures)
       - ChangeType string (INIT, TABLE_ADDED, TABLE_REMOVED, TABLE_MODIFIED, etc.)
       - ObjectType string (TABLE, COLUMN, INDEX, EXTENSION)
       - ObjectName string
       - ChangeDetails string (JSON describing the change)
       - PreviousValue string, NewValue string

    Add `db` and `json` tags following patterns.
  </action>
  <acceptance_criteria>
    - File exists at backend/pkg/models/inventory_models.go
    - TableInventory struct with RowCount, TotalSizeMb fields
    - ColumnInventory struct with data type fields
    - IndexInventory struct with usage stats and UsageStatus
    - ExtensionInventory struct with version fields
    - SchemaVersion struct for change tracking
    - All structs have `db` and `json` tags
    - File compiles
  </acceptance_criteria>
  <verify>
    <automated>go build ./backend/pkg/models</automated>
  </verify>
  <done>
    Inventory models file created with 5 structs covering INV-01 through INV-05.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inventory tables to database migration</name>
  <files>
    backend/migrations/031_replication_tables.sql
  </files>
  <read_first>
    - backend/migrations/031_replication_tables.sql (existing)
    - backend/pkg/models/inventory_models.go (struct fields)
  </read_first>
  <action>
    Append to migration:

    1. metrics_table_inventory table:
       - time TIMESTAMPTZ NOT NULL
       - collector_id UUID NOT NULL REFERENCES collectors(id)
       - database_name VARCHAR(255), schema_name VARCHAR(255), table_name VARCHAR(255), table_type VARCHAR(50)
       - row_count BIGINT, total_size_mb BIGINT, table_size_mb BIGINT, index_size_mb BIGINT, toast_size_mb BIGINT
       - has_oids BOOLEAN, table_oid BIGINT
       - CREATE INDEX idx_table_inventory_collector

    2. metrics_column_inventory table:
       - time TIMESTAMPTZ NOT NULL
       - collector_id UUID NOT NULL REFERENCES collectors(id)
       - database_name VARCHAR(255), schema_name VARCHAR(255), table_name VARCHAR(255), column_name VARCHAR(255)
       - data_type VARCHAR(100), is_nullable BOOLEAN, column_default TEXT
       - ordinal_position INT, character_max_length INT, numeric_precision INT, numeric_scale INT
       - is_primary_key BOOLEAN, is_foreign_key BOOLEAN
       - CREATE INDEX idx_column_inventory_collector

    3. metrics_index_inventory table:
       - time TIMESTAMPTZ NOT NULL
       - collector_id UUID NOT NULL REFERENCES collectors(id)
       - database_name VARCHAR(255), schema_name VARCHAR(255), table_name VARCHAR(255), index_name VARCHAR(255)
       - index_definition TEXT, index_size_mb BIGINT
       - idx_scan BIGINT, idx_tup_read BIGINT, idx_tup_fetch BIGINT
       - usage_status VARCHAR(50), is_primary BOOLEAN, is_unique BOOLEAN, index_oid BIGINT
       - CREATE INDEX idx_index_inventory_collector

    4. metrics_extension_inventory table:
       - time TIMESTAMPTZ NOT NULL
       - collector_id UUID NOT NULL REFERENCES collectors(id)
       - database_name VARCHAR(255)
       - extension_name VARCHAR(255), extension_version VARCHAR(100)
       - extension_owner VARCHAR(255), extension_schema VARCHAR(255)
       - is_relocatable BOOLEAN, description TEXT
       - CREATE INDEX idx_extension_inventory_collector

    5. metrics_schema_versions table:
       - time TIMESTAMPTZ NOT NULL
       - collector_id UUID NOT NULL REFERENCES collectors(id)
       - database_name VARCHAR(255)
       - version_hash VARCHAR(64) -- MD5 hash
       - change_type VARCHAR(50), object_type VARCHAR(50), object_name VARCHAR(255)
       - change_details TEXT, previous_value TEXT, new_value TEXT
       - CREATE INDEX idx_schema_versions_collector

    Add create_hypertable for time-series tables.
  </action>
  <acceptance_criteria>
    - All 5 inventory tables created
    - All tables have proper indexes
    - Time-series tables have hypertables
    - Foreign keys reference collectors(id)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "metrics_table_inventory\|metrics_column_inventory\|metrics_index_inventory\|metrics_extension_inventory\|metrics_schema_versions" backend/migrations/031_replication_tables.sql | grep -q "^5$"</automated>
  </verify>
  <done>
    All inventory tables added to migration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create inventory store</name>
  <files>
    backend/internal/storage/inventory_store.go
  </files>
  <read_first>
    - backend/internal/storage/metrics_store.go (pattern reference)
    - backend/internal/storage/postgres.go
    - backend/pkg/models/inventory_models.go
  </read_first>
  <action>
    Create backend/internal/storage/inventory_store.go:

    1. StoreTableInventory(ctx context.Context, tables []*models.TableInventory) error
       - Batch insert into metrics_table_inventory

    2. GetTableInventory(ctx context.Context, collectorID uuid.UUID, database *string, schema *string, limit int, offset int) ([]*models.TableInventory, error)
       - Filter by database and schema if provided
       - Order by total_size_mb DESC, then schema_name, table_name

    3. StoreColumnInventory(ctx context.Context, columns []*models.ColumnInventory) error

    4. GetColumnInventory(ctx context.Context, collectorID uuid.UUID, database *string, table *string, limit int, offset int) ([]*models.ColumnInventory, error)
       - Filter by database and table name if provided

    5. StoreIndexInventory(ctx context.Context, indexes []*models.IndexInventory) error

    6. GetIndexInventory(ctx context.Context, collectorID uuid.UUID, database *string, table *string, limit int, offset int) ([]*models.IndexInventory, error)

    7. StoreExtensionInventory(ctx context.Context, extensions []*models.ExtensionInventory) error

    8. GetExtensionInventory(ctx context.Context, collectorID uuid.UUID, database *string, limit int, offset int) ([]*models.ExtensionInventory, error)

    9. StoreSchemaVersion(ctx context.Context, version *models.SchemaVersion) error

    10. GetSchemaVersions(ctx context.Context, collectorID uuid.UUID, database *string, limit int) ([]*models.SchemaVersion, error)
        - Get recent schema changes
        - Order by time DESC

    11. CalculateSchemaHash(ctx context.Context, collectorID uuid.UUID, database string) (string, error)
        - Calculate MD5 hash of all object signatures
        - Used for change detection

    Use prepared statements, transactions, and proper error handling.
  </action>
  <acceptance_criteria>
    - All 11 storage functions exist
    - Functions use prepared statements
    - Get functions support filtering
    - Change tracking via schema hash calculation
  </acceptance_criteria>
  <verify>
    <automated>go build ./backend/internal/storage</automated>
  </verify>
  <done>
    Inventory store created with full CRUD operations and schema change tracking.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create inventory API handlers</name>
  <files>
    backend/internal/api/handlers_inventory.go
  </files>
  <read_first>
    - backend/internal/api/handlers_metrics.go (pattern reference)
    - backend/internal/storage/inventory_store.go
  </read_first>
  <action>
    Create backend/internal/api/handlers_inventory.go:

    1. handleGetTableInventory(c *gin.Context)
       - Parse collector_id (required)
       - Parse database, schema query params (optional filters)
       - Parse limit, offset
       - Call s.postgres.GetTableInventory
       - Return MetricsResponse{MetricType: "table_inventory", ...}

    2. handleGetColumnInventory(c *gin.Context)
       - Parse collector_id, database, table params
       - Call s.postgres.GetColumnInventory
       - Return MetricsResponse{MetricType: "column_inventory", ...}

    3. handleGetIndexInventory(c *gin.Context)
       - Parse collector_id, database, table params
       - Call s.postgres.GetIndexInventory
       - Return MetricsResponse{MetricType: "index_inventory", ...}

    4. handleGetExtensionInventory(c *gin.Context)
       - Parse collector_id, database params
       - Call s.postgres.GetExtensionInventory
       - Return MetricsResponse{MetricType: "extension_inventory", ...}

    5. handleGetSchemaVersions(c *gin.Context)
       - Parse collector_id, database params
       - Call s.postgres.GetSchemaVersions
       - Return MetricsResponse{MetricType: "schema_versions", ...}

    Add Swagger annotations for each handler.
  </action>
  <acceptance_criteria>
    - All 5 handler functions exist
    - Handlers validate required params
    - Handlers support optional filters
    - Swagger annotations present
  </acceptance_criteria>
  <verify>
    <automated>go build ./backend/internal/api</automated>
  </verify>
  <done>
    Inventory API handlers created with 5 endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire inventory routes in server</name>
  <files>
    backend/internal/api/server.go
  </files>
  <read_first>
    - backend/internal/api/server.go
    - backend/internal/api/handlers_inventory.go
  </read_first>
  <action>
    In server.go collectors group, add inventory routes:

    ```go
    // Database inventory routes
    collectors.GET("/:id/inventory/tables", s.AuthMiddleware(), s.handleGetTableInventory)
    collectors.GET("/:id/inventory/columns", s.AuthMiddleware(), s.handleGetColumnInventory)
    collectors.GET("/:id/inventory/indexes", s.AuthMiddleware(), s.handleGetIndexInventory)
    collectors.GET("/:id/inventory/extensions", s.AuthMiddleware(), s.handleGetExtensionInventory)
    collectors.GET("/:id/inventory/schema-versions", s.AuthMiddleware(), s.handleGetSchemaVersions)
    ```
  </action>
  <acceptance_criteria>
    - 5 inventory routes registered
    - All use s.AuthMiddleware()
    - Routes in collectors group
  </acceptance_criteria>
  <verify>
    <automated>grep -c "handleGetTableInventory\|handleGetColumnInventory\|handleGetIndexInventory\|handleGetExtensionInventory\|handleGetSchemaVersions" backend/internal/api/server.go | grep -q "^5$"</automated>
  </verify>
  <done>
    Server has 5 inventory routes.
  </done>
</task>

<task type="auto">
  <name>Task 6: Enhance C++ schema plugin with table sizes</name>
  <files>
    collector/src/schema_plugin.cpp
  </files>
  <read_first>
    - collector/src/schema_plugin.cpp (existing implementation)
    - backend/pkg/models/inventory_models.go (required fields)
  </read_first>
  <action>
    Enhance collectTableSchema() in schema_plugin.cpp:

    1. Add table size query:
       ```cpp
       const char* query = R"(
           SELECT
               n.nspname as schema_name,
               c.relname as table_name,
               CASE c.relkind WHEN 'r' THEN 'BASE TABLE' WHEN 'v' THEN 'VIEW' END as table_type,
               COALESCE(s.n_live_tup, 0) as row_count,
               pg_total_relation_size(c.oid) / 1024 / 1024 as total_size_mb,
               pg_relation_size(c.oid) / 1024 / 1024 as table_size_mb,
               COALESCE((SELECT sum(pg_relation_size(indexrelid)) / 1024 / 1024 
                        FROM pg_index WHERE indrelid = c.oid), 0) as index_size_mb,
               COALESCE(pg_relation_size(c.reltoastrelid) / 1024 / 1024, 0) as toast_size_mb,
               c.reloptions @> ARRAY['oids=on'] as has_oids,
               c.oid as table_oid
           FROM pg_class c
           JOIN pg_namespace n ON n.oid = c.relnamespace
           LEFT JOIN pg_stat_user_tables s ON s.relid = c.oid
           WHERE c.relkind IN ('r', 'v')
               AND n.nspname NOT IN ('pg_catalog', 'information_schema', 'pganalytics')
           ORDER BY n.nspname, c.relname
       )";
       ```

    2. Enhance collectIndexInfo() to include index size:
       - Add pg_relation_size(indexrelid) / 1024 / 1024 as index_size_mb
       - Add index_oid field

    3. Modify execute() to use new table format:
       - Update returned JSON structure to match inventory models
       - Include row_count, sizes, and oid for change tracking

    Keep backward compatibility if the plugin is used elsewhere.
  </action>
  <acceptance_criteria>
    - collectTableSchema includes row_count from pg_stat_user_tables
    - collectTableSchema includes total_size_mb, table_size_mb, index_size_mb
    - collectIndexInfo includes index_size_mb
    - execute() returns enhanced structure
    - Existing functionality preserved
  </acceptance_criteria>
  <verify>
    <automated>grep -c "pg_total_relation_size\|n_live_tup" collector/src/schema_plugin.cpp | grep -q "^2$"</automated>
  </verify>
  <done>
    Schema plugin enhanced with table sizes and row counts for inventory.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `go build ./backend/...` - must succeed
2. Verify all inventory tables in migration
3. Verify routes are registered
4. Verify C++ plugin compiles
</verification>

<success_criteria>
- User can GET /api/v1/collectors/{id}/inventory/tables and see tables with sizes and row counts
- User can GET /api/v1/collectors/{id}/inventory/columns and see all columns with types
- User can GET /api/v1/collectors/{id}/inventory/indexes and see indexes with usage status
- User can GET /api/v1/collectors/{id}/inventory/extensions and see extensions with versions
- User can GET /api/v1/collectors/{id}/inventory/schema-versions and see schema change history
- C++ collector collects table sizes and row counts
- Schema change tracking enabled via version hash comparison
</success_criteria>

<output>
After completion, create `.planning/phases/10-collector-backend-foundation/10-04-SUMMARY.md`
</output>
