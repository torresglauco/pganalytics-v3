---
phase: 13-frontend-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/classification.ts
  - frontend/src/api/classificationApi.ts
  - frontend/src/components/classification/ClassificationTable.tsx
  - frontend/src/components/classification/ClassificationFilters.tsx
  - frontend/src/components/classification/ClassificationSummary.tsx
  - frontend/src/components/classification/PatternBreakdownChart.tsx
  - frontend/src/pages/DataClassificationPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - UI-03
user_setup: []

must_haves:
  truths:
    - "User can view data classification reports with drill-down by database/table"
    - "User can filter results by pattern type (CPF, CNPJ, EMAIL, etc.)"
    - "User can filter results by category (PII, PCI, SENSITIVE)"
    - "User can see pattern breakdown chart with counts"
    - "User can see database summary with PII/PCI column counts"
  artifacts:
    - path: "frontend/src/pages/DataClassificationPage.tsx"
      provides: "Page component with drill-down navigation"
      exports: ["DataClassificationPage"]
    - path: "frontend/src/api/classificationApi.ts"
      provides: "API client for classification endpoints"
      exports: ["classificationApi"]
    - path: "frontend/src/components/classification/ClassificationTable.tsx"
      provides: "Table displaying classification results"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/DataClassificationPage.tsx"
      to: "/api/v1/collectors/{id}/classification"
      via: "classificationApi.getClassificationResults"
      pattern: "classificationApi\\.get"
    - from: "frontend/src/components/classification/PatternBreakdownChart.tsx"
      to: "frontend/src/pages/DataClassificationPage.tsx"
      via: "props: patternBreakdown data"
      pattern: "PatternBreakdown"
---

<objective>
Implement data classification report UI with drill-down and filtering capabilities.

Purpose: Enable users to view PII/PCI detection results with drill-down by database, schema, and table.
Output: DataClassificationPage with filters, table, summary cards, and pattern breakdown chart.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-ui/13-RESEARCH.md
@.planning/phases/13-frontend-ui/13-VALIDATION.md

<interfaces>
<!-- Backend API response types - Executor should use these directly -->

From backend/pkg/models/classification_models.go:
```go
type DataClassificationResult struct {
    Time              time.Time           `json:"time"`
    CollectorID       uuid.UUID           `json:"collector_id"`
    DatabaseName      string              `json:"database_name"`
    SchemaName        string              `json:"schema_name"`
    TableName         string              `json:"table_name"`
    ColumnName        string              `json:"column_name"`
    PatternType       string              `json:"pattern_type"` // CPF, CNPJ, EMAIL, PHONE, CREDIT_CARD, CUSTOM
    Category          string              `json:"category"`     // PII, PCI, SENSITIVE, CUSTOM
    Confidence        float64             `json:"confidence"`
    MatchCount        int64               `json:"match_count"`
    SampleValues      []string            `json:"sample_values"`
    RegulationMapping map[string][]string `json:"regulation_mapping"`
}

type ClassificationReportResponse struct {
    TotalDatabases    int                             `json:"total_databases"`
    TotalTables       int                             `json:"total_tables"`
    TotalColumns      int                             `json:"total_columns"`
    PiiColumns        int                             `json:"pii_columns"`
    PciColumns        int                             `json:"pci_columns"`
    SensitiveColumns  int                             `json:"sensitive_columns"`
    CustomColumns     int                             `json:"custom_columns"`
    PatternBreakdown  map[string]int64                `json:"pattern_breakdown"`
    CategoryBreakdown map[string]int64                `json:"category_breakdown"`
    DatabaseSummary   []DatabaseClassificationSummary `json:"database_summary,omitempty"`
}
```

API Endpoints:
- GET /api/v1/collectors/{id}/classification
- GET /api/v1/collectors/{id}/classification/report

Query params: database, schema, table, pattern_type, category, time_range, limit, offset
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create classification type definitions</name>
  <files>frontend/src/types/classification.ts</files>
  <read_first>
    - backend/pkg/models/classification_models.go
    - frontend/src/types/alertDashboard.ts (for pattern reference)
  </read_first>
  <acceptance_criteria>
    - grep -q "DataClassificationResult" frontend/src/types/classification.ts
    - grep -q "ClassificationReportResponse" frontend/src/types/classification.ts
    - grep -q "ClassificationFilter" frontend/src/types/classification.ts
  </acceptance_criteria>
  <action>
Create TypeScript type definitions that mirror backend classification models.

Define in frontend/src/types/classification.ts:
- DataClassificationResult (all fields from backend)
- ClassificationReportResponse (aggregated report)
- DatabaseClassificationSummary (per-database summary)
- ClassificationFilter (for query parameters)
- PatternType enum: CPF, CNPJ, EMAIL, PHONE, CREDIT_CARD, CUSTOM
- Category enum: PII, PCI, SENSITIVE, CUSTOM

Export all types for use in API client and components.
  </action>
  <verify>
    <automated>grep -q "DataClassificationResult" frontend/src/types/classification.ts && grep -q "PatternType" frontend/src/types/classification.ts</automated>
  </verify>
  <done>TypeScript types created matching backend models</done>
</task>

<task type="auto">
  <name>Task 2: Create classification API client</name>
  <files>frontend/src/api/classificationApi.ts</files>
  <read_first>
    - frontend/src/api/alertDashboardApi.ts
    - frontend/src/types/classification.ts
    - backend/internal/api/handlers_data_classification.go
  </read_first>
  <acceptance_criteria>
    - grep -q "getClassificationResults" frontend/src/api/classificationApi.ts
    - grep -q "getClassificationReport" frontend/src/api/classificationApi.ts
    - grep -q "getCustomPatterns" frontend/src/api/classificationApi.ts
  </acceptance_criteria>
  <action>
Create API client for classification endpoints.

Implement classificationApi object with methods:
- getClassificationResults(collectorId, filters): Promise<DataClassificationResult[]>
  - Build query params from filters object
  - GET /api/v1/collectors/{id}/classification
- getClassificationReport(collectorId): Promise<ClassificationReportResponse>
  - GET /api/v1/collectors/{id}/classification/report
- getCustomPatterns(): Promise<CustomPattern[]>
  - GET /api/v1/classification/patterns

Filter object supports:
- database, schema, table (string)
- pattern_type, category (string)
- time_range (default: '24h')
- limit, offset (pagination)
  </action>
  <verify>
    <automated>grep -q "getClassificationResults" frontend/src/api/classificationApi.ts && grep -q "API_BASE" frontend/src/api/classificationApi.ts</automated>
  </verify>
  <done>API client created with classification endpoint methods</done>
</task>

<task type="auto">
  <name>Task 3: Create classification components</name>
  <files>
    frontend/src/components/classification/ClassificationTable.tsx,
    frontend/src/components/classification/ClassificationFilters.tsx,
    frontend/src/components/classification/ClassificationSummary.tsx,
    frontend/src/components/classification/PatternBreakdownChart.tsx
  </files>
  <read_first>
    - frontend/src/components/AlertsTable.tsx
    - frontend/src/components/AlertFiltersPanel.tsx
    - frontend/src/components/charts/GaugeChart.tsx
    - frontend/src/components/dashboard/MetricCard.tsx
  </read_first>
  <acceptance_criteria>
    - grep -q "ClassificationTable" frontend/src/components/classification/ClassificationTable.tsx
    - grep -q "ClassificationFilters" frontend/src/components/classification/ClassificationFilters.tsx
    - grep -q "PatternBreakdownChart" frontend/src/components/classification/PatternBreakdownChart.tsx
    - grep -q "ClassificationSummary" frontend/src/components/classification/ClassificationSummary.tsx
  </acceptance_criteria>
  <action>
Create classification UI components.

ClassificationTable.tsx:
- Use @tanstack/react-table (already installed)
- Columns: database_name, schema_name, table_name, column_name, pattern_type, category, confidence, match_count
- Add badge styling for pattern_type (different colors per type)
- Add badge styling for category (PII=blue, PCI=red, SENSITIVE=amber, CUSTOM=gray)
- Confidence displayed as percentage with color coding
- Click row to drill down (filter by database/schema/table)

ClassificationFilters.tsx:
- Database select dropdown (populated from unique values)
- Schema select dropdown (filtered by selected database)
- Table select dropdown (filtered by selected schema)
- Pattern type multi-select
- Category multi-select
- Time range selector (1h, 24h, 7d, 30d)
- Apply/Reset buttons

ClassificationSummary.tsx:
- Display summary cards in grid layout
- Cards: Total Databases, Total Tables, Total Columns, PII Columns, PCI Columns, Sensitive Columns
- Use MetricCard component pattern
- Click card to filter by that metric

PatternBreakdownChart.tsx:
- Use Recharts PieChart (existing dependency)
- Display pattern_breakdown data as pie chart
- Color mapping: CPF=amber, CNPJ=emerald, EMAIL=blue, PHONE=violet, CREDIT_CARD=red, CUSTOM=gray
- Show legend with counts
- Interactive: click segment to filter
  </action>
  <verify>
    <automated>cd frontend && grep -q "ClassificationTable" src/components/classification/ClassificationTable.tsx && grep -q "PieChart" src/components/classification/PatternBreakdownChart.tsx</automated>
  </verify>
  <done>Classification components created with table, filters, summary, and chart</done>
</task>

<task type="auto">
  <name>Task 4: Create DataClassificationPage and wire routes</name>
  <files>frontend/src/pages/DataClassificationPage.tsx, frontend/src/App.tsx, frontend/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - frontend/src/pages/AlertsDashboard.tsx
    - frontend/src/App.tsx
    - frontend/src/components/layout/Sidebar.tsx
    - frontend/src/api/classificationApi.ts
  </read_first>
  <acceptance_criteria>
    - grep -q "DataClassificationPage" frontend/src/App.tsx
    - grep -q "/classification" frontend/src/App.tsx
    - grep -q "Classification" frontend/src/components/layout/Sidebar.tsx
    - grep -q "DataClassificationPage" frontend/src/pages/DataClassificationPage.tsx
  </acceptance_criteria>
  <action>
Create the page component and wire into routing.

DataClassificationPage.tsx:
- Use useParams to get collectorId from route
- Fetch classification report on mount for summary
- Fetch classification results with current filters
- Handle drill-down: clicking database/schema/table updates filters
- Breadcrumb navigation showing current drill-down path
- Combine ClassificationSummary, PatternBreakdownChart, ClassificationFilters, ClassificationTable
- Export/Refresh buttons in header
- Handle loading and error states

App.tsx routing:
- Add route: `/classification/:collectorId` -> DataClassificationPage
- Place in authenticated routes section

Sidebar.tsx:
- Add "Data Classification" navigation item with icon (Shield or Database)
- Link to /classification route
- Place near other monitoring items
  </action>
  <verify>
    <automated>cd frontend && grep -q "DataClassificationPage" src/App.tsx && grep -q "classification" src/App.tsx</automated>
  </verify>
  <done>Page created and wired with routing, accessible from sidebar</done>
</task>

</tasks>

<verification>
- Run `cd frontend && npm run type-check` - TypeScript compiles without errors
- Run `cd frontend && npm run lint` - No ESLint errors
- Run `cd frontend && npm test` - Tests pass
- Manual: Navigate to /classification/{collectorId} and verify table, filters, and chart render
</verification>

<success_criteria>
- User can view data classification results in a filterable table
- User can drill down by database -> schema -> table
- User can see pattern breakdown chart
- User can filter by pattern type and category
- Summary cards show aggregated counts
- Breadcrumb navigation shows current drill-down path
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-ui/13-02-SUMMARY.md`
</output>