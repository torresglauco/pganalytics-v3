---
phase: 10-collector-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pkg/models/replication_models.go
  - backend/internal/storage/replication_store.go
  - backend/internal/api/handlers_replication.go
  - backend/migrations/031_replication_tables.sql
  - backend/internal/api/server.go
autonomous: true
requirements:
  - REP-01
  - REP-04
must_haves:
  truths:
    - "User can view streaming replication status with write/flush/replay lag metrics"
    - "User can view replication slots with WAL retention and active status"
    - "Replication metrics are stored in TimescaleDB hypertables for time-series queries"
  artifacts:
    - path: "backend/pkg/models/replication_models.go"
      provides: "Data structures for replication metrics"
      exports: ["ReplicationStatus", "ReplicationSlot", "ReplicationMetricsResponse"]
    - path: "backend/internal/storage/replication_store.go"
      provides: "Database operations for replication metrics"
      exports: ["StoreReplicationMetrics", "GetReplicationMetrics", "StoreReplicationSlots", "GetReplicationSlots"]
    - path: "backend/internal/api/handlers_replication.go"
      provides: "HTTP handlers for replication endpoints"
      exports: ["handleGetReplicationMetrics", "handleGetReplicationSlots"]
    - path: "backend/migrations/031_replication_tables.sql"
      provides: "Database schema for replication metrics"
      contains: "CREATE TABLE.*metrics_replication"
  key_links:
    - from: "backend/internal/api/handlers_replication.go"
      to: "backend/internal/storage/replication_store.go"
      via: "s.postgres.GetReplicationMetrics"
      pattern: "GetReplicationMetrics"
    - from: "backend/internal/api/server.go"
      to: "backend/internal/api/handlers_replication.go"
      via: "route registration"
      pattern: "collectors.GET.*replication"
---

<objective>
Implement backend storage and API for PostgreSQL streaming replication monitoring. This enables users to view replication status with lag metrics and replication slots with WAL retention information.

Purpose: Provide real-time visibility into PostgreSQL replication health and detect replication lag issues before they impact production.
Output: Working API endpoints for replication metrics with TimescaleDB storage.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing patterns the executor must follow -->

From backend/pkg/models/metrics_models.go pattern:
```go
type MetricsResponse struct {
    MetricType string      `json:"metric_type"`
    Count      int         `json:"count"`
    Timestamp  time.Time   `json:"timestamp"`
    Data       interface{} `json:"data"`
}
```

From backend/internal/storage/metrics_store.go pattern:
```go
func (p *PostgresDB) StoreSchemaMetrics(ctx context.Context, tables []*models.SchemaTable, ...) error {
    tx, err := p.db.BeginTx(ctx, nil)
    // ... prepared statement with ON CONFLICT DO NOTHING
    return tx.Commit()
}
```

From backend/internal/api/handlers_metrics.go pattern:
```go
func (s *Server) handleGetSchemaMetrics(c *gin.Context) {
    collectorIDStr := c.Param("collector_id")
    collectorID, err := uuid.Parse(collectorIDStr)
    // ... error handling, query params
    metrics, err := s.postgres.GetSchemaMetrics(ctx, collectorID, dbPtr, limit, offset)
    resp := &models.MetricsResponse{MetricType: "pg_schema", ...}
    c.JSON(http.StatusOK, resp)
}
```

From backend/internal/api/server.go route registration:
```go
collectors.GET("/:id/schema", s.AuthMiddleware(), s.handleGetSchemaMetrics)
collectors.GET("/:id/extensions", s.AuthMiddleware(), s.handleGetExtensionMetrics)
```

From collector/src/replication_plugin.cpp (already collecting):
- Replication status: server_pid, application_name, state, sync_state, write_lsn, flush_lsn, replay_lsn, write_lag_ms, flush_lag_ms, replay_lag_ms, behind_by_mb
- Replication slots: slot_name, slot_type, active, restart_lsn, confirmed_flush_lsn, wal_retained_mb, backend_pid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create replication models</name>
  <files>backend/pkg/models/replication_models.go</files>
  <read_first>
    - backend/pkg/models/metrics_models.go
  </read_first>
  <action>
Create replication_models.go with the following structures:

1. ReplicationStatus struct with JSON and DB tags:
   - ID string, CollectorID uuid.UUID, Timestamp time.Time
   - ServerPID int64, Usename string, ApplicationName string
   - State string (streaming, catchup, etc.), SyncState string (sync, async, potential, quorum)
   - WriteLsn string, FlushLsn string, ReplayLsn string
   - WriteLagMs int64, FlushLagMs int64, ReplayLagMs int64 (milliseconds)
   - BehindByMb int64 (bytes behind converted to MB)
   - ClientAddr string, BackendStart *time.Time

2. ReplicationSlot struct:
   - ID string, CollectorID uuid.UUID, DatabaseName string, Timestamp time.Time
   - SlotName string, SlotType string (physical, logical)
   - Active bool, RestartLsn string, ConfirmedFlushLsn string
   - WalRetainedMb int64, BackendPid int64, BytesRetained int64

3. ReplicationMetricsResponse struct:
   - ReplicationStatus []*ReplicationStatus
   - ReplicationSlots []*ReplicationSlot
   - WalStatus json.RawMessage (from collector's wal_status)

Follow exact naming conventions from metrics_models.go - use `json:"field_name"` and `db:"field_name"` tags.
  </action>
  <verify>
    <automated>go build ./backend/pkg/models</automated>
  </verify>
  <done>
    - File backend/pkg/models/replication_models.go exists
    - Contains ReplicationStatus, ReplicationSlot, ReplicationMetricsResponse structs
    - All structs have CollectorID uuid.UUID field
    - All time fields use *time.Time for nullable, time.Time for required
  </done>
</task>

<task type="auto">
  <name>Task 2: Create replication database migration</name>
  <files>backend/migrations/031_replication_tables.sql</files>
  <read_first>
    - backend/migrations/011_schema_metrics.sql.disabled
  </read_first>
  <action>
Create migration 031_replication_tables.sql with:

1. metrics_replication_status table:
   - time TIMESTAMPTZ NOT NULL
   - collector_id UUID NOT NULL REFERENCES collectors(id)
   - server_pid BIGINT, usename VARCHAR(255), application_name VARCHAR(255)
   - state VARCHAR(50), sync_state VARCHAR(20)
   - write_lsn VARCHAR(50), flush_lsn VARCHAR(50), replay_lsn VARCHAR(50)
   - write_lag_ms BIGINT, flush_lag_ms BIGINT, replay_lag_ms BIGINT
   - behind_by_mb BIGINT, client_addr VARCHAR(100)
   - backend_start TIMESTAMPTZ

2. metrics_replication_slots table:
   - time TIMESTAMPTZ NOT NULL
   - collector_id UUID NOT NULL REFERENCES collectors(id)
   - database_name VARCHAR(255)
   - slot_name VARCHAR(255), slot_type VARCHAR(20)
   - active BOOLEAN, restart_lsn VARCHAR(50), confirmed_flush_lsn VARCHAR(50)
   - wal_retained_mb BIGINT, backend_pid BIGINT, bytes_retained BIGINT

3. TimescaleDB hypertables:
   - SELECT create_hypertable('metrics_replication_status', 'time', if_not_exists => TRUE);
   - SELECT create_hypertable('metrics_replication_slots', 'time', if_not_exists => TRUE);

4. Indexes:
   - CREATE INDEX idx_replication_status_collector ON metrics_replication_status(collector_id, time DESC);
   - CREATE INDEX idx_replication_slots_collector ON metrics_replication_slots(collector_id, time DESC);

5. Retention policies (90 days):
   - SELECT add_retention_policy('metrics_replication_status', INTERVAL '90 days', if_not_exists => TRUE);
   - SELECT add_retention_policy('metrics_replication_slots', INTERVAL '90 days', if_not_exists => TRUE);
  </action>
  <verify>
    <automated>grep -c "metrics_replication_status" backend/migrations/031_replication_tables.sql</automated>
  </verify>
  <done>
    - File backend/migrations/031_replication_tables.sql exists
    - Contains CREATE TABLE for metrics_replication_status with all columns
    - Contains CREATE TABLE for metrics_replication_slots with all columns
    - Contains create_hypertable calls for both tables
    - Contains CREATE INDEX statements
    - Contains add_retention_policy calls
  </done>
</task>

<task type="auto">
  <name>Task 3: Create replication store</name>
  <files>backend/internal/storage/replication_store.go</files>
  <read_first>
    - backend/internal/storage/metrics_store.go
    - backend/pkg/models/replication_models.go
  </read_first>
  <action>
Create replication_store.go following exact patterns from metrics_store.go:

1. StoreReplicationMetrics(ctx context.Context, status []*models.ReplicationStatus) error
   - Begin transaction
   - Prepare statement: INSERT INTO metrics_replication_status (time, collector_id, server_pid, usename, application_name, state, sync_state, write_lsn, flush_lsn, replay_lsn, write_lag_ms, flush_lag_ms, replay_lag_ms, behind_by_mb, client_addr, backend_start) VALUES ($1-$16) ON CONFLICT DO NOTHING
   - Execute for each status item with time.Now()
   - Commit transaction
   - Return apperrors.DatabaseError on failure

2. GetReplicationMetrics(ctx context.Context, collectorID uuid.UUID, limit int, offset int) (*models.ReplicationMetricsResponse, error)
   - Query metrics_replication_status WHERE collector_id = $1 ORDER BY time DESC LIMIT $2 OFFSET $3
   - Scan into ReplicationStatus structs
   - Return ReplicationMetricsResponse{ReplicationStatus: results}

3. StoreReplicationSlots(ctx context.Context, slots []*models.ReplicationSlot) error
   - Same pattern as StoreReplicationMetrics
   - INSERT INTO metrics_replication_slots (time, collector_id, database_name, slot_name, slot_type, active, restart_lsn, confirmed_flush_lsn, wal_retained_mb, backend_pid, bytes_retained) VALUES ($1-$11) ON CONFLICT DO NOTHING

4. GetReplicationSlots(ctx context.Context, collectorID uuid.UUID, limit int, offset int) ([]*models.ReplicationSlot, error)
   - Query metrics_replication_slots WHERE collector_id = $1 ORDER BY time DESC LIMIT $2 OFFSET $3

Use defer tx.Rollback() pattern and proper error handling with apperrors.DatabaseError.
  </action>
  <verify>
    <automated>go build ./backend/internal/storage</automated>
  </verify>
  <done>
    - File backend/internal/storage/replication_store.go exists
    - Contains StoreReplicationMetrics, GetReplicationMetrics functions
    - Contains StoreReplicationSlots, GetReplicationSlots functions
    - All functions use ctx context.Context as first parameter
    - All functions use prepared statements with ON CONFLICT DO NOTHING
    - Uses apperrors.DatabaseError for error wrapping
  </done>
</task>

<task type="auto">
  <name>Task 4: Create replication API handlers</name>
  <files>backend/internal/api/handlers_replication.go</files>
  <read_first>
    - backend/internal/api/handlers_metrics.go
    - backend/internal/storage/replication_store.go
  </read_first>
  <action>
Create handlers_replication.go following exact patterns from handlers_metrics.go:

1. handleGetReplicationMetrics(c *gin.Context)
   - Parse collector_id from path param, return 400 if invalid UUID
   - Parse limit (default 100, max 1000) and offset (default 0) from query
   - Call s.postgres.GetReplicationMetrics(ctx, collectorID, limit, offset)
   - Return MetricsResponse{MetricType: "pg_replication", Count: len, Timestamp: time.Now(), Data: metrics}
   - Handle errors with c.JSON(err.(*apperrors.AppError).StatusCode, err)

2. handleGetReplicationSlots(c *gin.Context)
   - Same pattern as handleGetReplicationMetrics
   - Call s.postgres.GetReplicationSlots(ctx, collectorID, limit, offset)
   - Return MetricsResponse{MetricType: "pg_replication_slots", ...}

3. Add Swagger annotations:
   - @Summary Get Replication Metrics
   - @Description Get streaming replication status with lag metrics
   - @Tags Replication
   - @Produce json
   - @Security Bearer
   - @Param collector_id path string true "Collector ID"
   - @Param limit query int false "Result limit" default(100)
   - @Param offset query int false "Result offset" default(0)
   - @Success 200 {object} models.MetricsResponse
   - @Failure 400 {object} apperrors.AppError
   - @Router /api/v1/collectors/{collector_id}/replication [get]
  </action>
  <verify>
    <automated>go build ./backend/internal/api</automated>
  </verify>
  <done>
    - File backend/internal/api/handlers_replication.go exists
    - Contains handleGetReplicationMetrics function
    - Contains handleGetReplicationSlots function
    - Both functions validate collector_id with uuid.Parse
    - Both functions return MetricsResponse with correct MetricType
    - Swagger annotations present on both handlers
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire replication routes in server</name>
  <files>backend/internal/api/server.go</files>
  <read_first>
    - backend/internal/api/server.go
    - backend/internal/api/handlers_replication.go
  </read_first>
  <action>
In server.go, find the collectors route group (around line 365 where other metrics routes are registered). Add two new routes following the existing pattern:

1. In the collectors := api.Group("/collectors") block, after existing metric routes:
   ```go
   collectors.GET("/:id/replication", s.AuthMiddleware(), s.handleGetReplicationMetrics)
   collectors.GET("/:id/replication-slots", s.AuthMiddleware(), s.handleGetReplicationSlots)
   ```

2. Ensure routes are placed before the DELETE route, following the pattern:
   - GET routes (list, detail, metrics)
   - POST/PUT routes
   - DELETE routes

The route registration should be in the same block as:
   collectors.GET("/:id/schema", s.AuthMiddleware(), s.handleGetSchemaMetrics)
   collectors.GET("/:id/extensions", s.AuthMiddleware(), s.handleGetExtensionMetrics)
  </action>
  <verify>
    <automated>grep -c "handleGetReplicationMetrics" backend/internal/api/server.go</automated>
  </verify>
  <done>
    - server.go contains collectors.GET("/:id/replication", ...) route
    - server.go contains collectors.GET("/:id/replication-slots", ...) route
    - Both routes use s.AuthMiddleware()
    - Routes are registered in the collectors group
  </done>
</task>

</tasks>

<verification>
Run the following to verify the implementation:

1. Build verification:
   ```bash
   go build ./backend/...
   ```

2. Route verification:
   ```bash
   grep "replication" backend/internal/api/server.go
   ```

3. Migration verification:
   ```bash
   grep -c "metrics_replication" backend/migrations/031_replication_tables.sql
   ```
</verification>

<success_criteria>
- User can GET /api/v1/collectors/{id}/replication and receive JSON with replication status including lag metrics
- User can GET /api/v1/collectors/{id}/replication-slots and receive JSON with slot information including WAL retention
- Response format matches MetricsResponse structure with MetricType field
- All endpoints require authentication via Bearer token
- Database tables created with TimescaleDB hypertables and retention policies
</success_criteria>

<output>
After completion, create `.planning/phases/10-collector-backend-foundation/10-01-SUMMARY.md`
</output>