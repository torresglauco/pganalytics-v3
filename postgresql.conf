# PostgreSQL Configuration for pgAnalytics
# Enable pg_stat_statements for query statistics collection
shared_preload_libraries = 'pg_stat_statements'

# Allow connections from any network interface (Docker network)
listen_addresses = '*'
