---
phase: 14-testing-quality
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/e2e/tests/12-replication-topology.spec.ts
  - frontend/e2e/tests/13-data-classification.spec.ts
  - frontend/e2e/tests/14-host-monitoring.spec.ts
  - frontend/e2e/pages/ReplicationTopologyPage.ts
  - frontend/e2e/pages/DataClassificationPage.ts
  - frontend/e2e/pages/HostInventoryPage.ts
autonomous: true
requirements:
  - TEST-05
must_haves:
  truths:
    - "End-to-end tests cover critical user flows for monitoring features"
    - "User can view replication topology graph in browser"
    - "User can navigate data classification page and filter results"
    - "User can view host inventory and filter by status"
    - "All E2E tests pass in CI with Playwright"
  artifacts:
    - path: "frontend/e2e/tests/12-replication-topology.spec.ts"
      provides: "Replication topology E2E tests"
      min_lines: 80
      exports: ["test"]
    - path: "frontend/e2e/tests/13-data-classification.spec.ts"
      provides: "Data classification E2E tests"
      min_lines: 100
      exports: ["test"]
    - path: "frontend/e2e/tests/14-host-monitoring.spec.ts"
      provides: "Host monitoring E2E tests"
      min_lines: 100
      exports: ["test"]
    - path: "frontend/e2e/pages/ReplicationTopologyPage.ts"
      provides: "Replication topology page object"
      min_lines: 50
    - path: "frontend/e2e/pages/DataClassificationPage.ts"
      provides: "Data classification page object"
      min_lines: 60
    - path: "frontend/e2e/pages/HostInventoryPage.ts"
      provides: "Host inventory page object"
      min_lines: 60
  key_links:
    - from: "frontend/e2e/tests/12-replication-topology.spec.ts"
      to: "frontend/e2e/pages/ReplicationTopologyPage.ts"
      via: "import"
      pattern: "new ReplicationTopologyPage"
    - from: "frontend/e2e/tests/13-data-classification.spec.ts"
      to: "frontend/e2e/pages/DataClassificationPage.ts"
      via: "import"
      pattern: "new DataClassificationPage"
    - from: "frontend/e2e/tests/14-host-monitoring.spec.ts"
      to: "frontend/e2e/pages/HostInventoryPage.ts"
      via: "import"
      pattern: "new HostInventoryPage"
---

<objective>
Create end-to-end tests for critical user flows through monitoring features.

Purpose: Verify complete user journeys work in real browser environment.
Output: Playwright E2E tests and page object models for new features.
</objective>

<execution_context>
@/Users/glauco.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glauco.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-testing-quality/14-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing E2E tests -->

From frontend/e2e/tests/01-login-logout.spec.ts (existing pattern):
```typescript
import { test, expect } from '@playwright/test'
import { LoginPage } from '../pages/LoginPage'
import { DashboardPage } from '../pages/DashboardPage'

test.describe('Feature Name', () => {
  let loginPage: LoginPage

  test.beforeEach(async ({ page }) => {
    loginPage = new LoginPage(page)
    await loginPage.goto()
    await loginPage.login('admin', 'admin')
  })

  test('should do something', async ({ page }) => {
    await expect(page.locator('selector')).toBeVisible()
  })
})
```

From frontend/e2e/pages/LoginPage.ts:
```typescript
export class LoginPage {
  constructor(private page: Page) {}

  async goto() {
    await this.page.goto('/login')
  }

  async login(username: string, password: string) {
    await this.page.fill('input[name="email"]', username)
    await this.page.fill('input[name="password"]', password)
    await this.page.click('button[type="submit"]')
  }

  async expectLoggedIn() {
    await expect(this.page).toHaveURL(/dashboard/)
  }
}
```

Page routes to test:
- /replication/topology/:collectorId
- /data-classification/:collectorId
- /host-inventory
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ReplicationTopologyPage page object</name>
  <files>
    frontend/e2e/pages/ReplicationTopologyPage.ts
  </files>
  <read_first>
    - frontend/e2e/pages/LoginPage.ts (for pattern)
    - frontend/e2e/pages/DashboardPage.ts
    - frontend/src/pages/ReplicationTopologyPage.tsx
  </read_first>
  <behavior>
    - Method: goto(collectorId) navigates to /replication/topology/:collectorId
    - Method: expectGraphVisible() waits for topology graph to render
    - Method: getNodeCount() returns number of visible nodes
    - Method: clickNode(nodeId) clicks on a specific node
    - Method: expectLegendVisible() verifies legend sidebar
    - Method: refresh() clicks the refresh button
  </behavior>
  <action>
    Create ReplicationTopologyPage.ts following the LoginPage.ts pattern. Implement navigation, graph visibility check, node interaction, and legend verification. Use data-testid selectors where available. Use Playwright's auto-waiting with expect(locator).toBeVisible(). Store selectors as private properties for maintainability.
  </action>
  <verify>
    <automated>grep -c "async " frontend/e2e/pages/ReplicationTopologyPage.ts || echo "0"</automated>
  </verify>
  <done>
    Page object created with 6+ async methods, navigation and interaction implemented
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DataClassificationPage page object</name>
  <files>
    frontend/e2e/pages/DataClassificationPage.ts
  </files>
  <read_first>
    - frontend/e2e/pages/LoginPage.ts (for pattern)
    - frontend/src/pages/DataClassificationPage.tsx
  </read_first>
  <behavior>
    - Method: goto(collectorId) navigates to /data-classification/:collectorId
    - Method: expectTableVisible() waits for classification table
    - Method: selectDatabase(name) selects database from dropdown
    - Method: selectPatternType(type) filters by pattern type
    - Method: getRowCount() returns number of table rows
    - Method: clickRow(index) clicks on table row for drill-down
    - Method: expectSummaryCardsVisible() verifies summary section
    - Method: exportReport() clicks export button
  </behavior>
  <action>
    Create DataClassificationPage.ts. Implement navigation, table interaction, filtering, and export methods. Use selectors for dropdown menus and table elements. Handle async operations with await. Use getByRole for table queries where possible.
  </action>
  <verify>
    <automated>grep -c "async " frontend/e2e/pages/DataClassificationPage.ts || echo "0"</automated>
  </verify>
  <done>
    Page object created with 8+ async methods, filtering and export implemented
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create HostInventoryPage page object</name>
  <files>
    frontend/e2e/pages/HostInventoryPage.ts
  </files>
  <read_first>
    - frontend/e2e/pages/LoginPage.ts (for pattern)
    - frontend/src/pages/HostInventoryPage.tsx
  </read_first>
  <behavior>
    - Method: goto() navigates to /host-inventory
    - Method: expectTableVisible() waits for host table
    - Method: searchByHostname(term) enters search term
    - Method: filterByStatus(status) selects status filter
    - Method: getRowCount() returns number of visible hosts
    - Method: clickHost(collectorId) clicks on host row
    - Method: expectDetailPanelOpen() verifies detail panel appears
    - Method: toggleAutoRefresh() toggles auto-refresh checkbox
    - Method: exportCsv() clicks export button
  </behavior>
  <action>
    Create HostInventoryPage.ts. Implement navigation, search, filtering, host selection, and export. Use selectors that match the actual component structure. Handle the detail panel interaction. Test auto-refresh toggle functionality.
  </action>
  <verify>
    <automated>grep -c "async " frontend/e2e/pages/HostInventoryPage.ts || echo "0"</automated>
  </verify>
  <done>
    Page object created with 9+ async methods, search/filter/detail/export implemented
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create replication topology E2E tests</name>
  <files>
    frontend/e2e/tests/12-replication-topology.spec.ts
  </files>
  <read_first>
    - frontend/e2e/tests/01-login-logout.spec.ts (for pattern)
    - frontend/e2e/pages/ReplicationTopologyPage.ts
  </read_first>
  <behavior>
    - Test: Login and navigate to topology page shows graph
    - Test: Graph displays correct number of nodes
    - Test: Legend sidebar is visible
    - Test: Refresh button reloads topology
    - Test: Node click shows metrics panel (if implemented)
    - Test: Handles missing collectorId with error message
  </behavior>
  <action>
    Create 12-replication-topology.spec.ts. Use test.beforeEach to login. Import ReplicationTopologyPage. Test the full flow: login, navigate to topology, verify graph renders. Test refresh functionality. Use expect(locator).toBeVisible() for assertions. Number test file with 12- prefix to maintain ordering.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --grep "Replication Topology" --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 6 test cases pass, graph renders, legend visible, refresh works
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create data classification E2E tests</name>
  <files>
    frontend/e2e/tests/13-data-classification.spec.ts
  </files>
  <read_first>
    - frontend/e2e/tests/01-login-logout.spec.ts (for pattern)
    - frontend/e2e/pages/DataClassificationPage.ts
  </read_first>
  <behavior>
    - Test: Login and navigate to classification page shows table
    - Test: Table displays classification results
    - Test: Summary cards show correct counts
    - Test: Database filter updates visible results
    - Test: Pattern type filter updates visible results
    - Test: Row click triggers drill-down navigation
    - Test: Export button is visible and clickable
    - Test: Pattern breakdown chart renders
  </behavior>
  <action>
    Create 13-data-classification.spec.ts. Test the full classification page flow. Verify summary cards display counts. Test filter dropdowns change the visible data. Test row click for drill-down. Use page object methods for interaction. Number test file with 13- prefix.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --grep "Data Classification" --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 8 test cases pass, filtering works, drill-down verified, export visible
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create host monitoring E2E tests</name>
  <files>
    frontend/e2e/tests/14-host-monitoring.spec.ts
  </files>
  <read_first>
    - frontend/e2e/tests/01-login-logout.spec.ts (for pattern)
    - frontend/e2e/pages/HostInventoryPage.ts
  </read_first>
  <behavior>
    - Test: Login and navigate to host inventory shows table
    - Test: Table displays all hosts with status
    - Test: Summary cards show up/down/unknown counts
    - Test: Search filters hosts by hostname
    - Test: Status filter shows only matching hosts
    - Test: Clicking host opens detail panel
    - Test: Auto-refresh toggle is present
    - Test: Export button triggers CSV download
    - Test: Empty state shown when no hosts exist
  </behavior>
  <action>
    Create 14-host-monitoring.spec.ts. Test the full host inventory flow. Verify status indicators display correctly. Test search functionality. Test status filter. Verify detail panel opens on host click. Test export triggers download. Number test file with 14- prefix.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --grep "Host" --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 9 test cases pass, search/filter work, detail panel opens, export downloads
  </done>
</task>

</tasks>

<verification>
1. Run all E2E tests: `cd frontend && npx playwright test`
2. Run specific test file: `npx playwright test 12-replication-topology.spec.ts`
3. Verify tests pass in headed mode: `npx playwright test --headed`
4. Generate test report: `npx playwright show-report`
5. All tests must pass in CI environment
</verification>

<success_criteria>
- [ ] frontend/e2e/pages/ReplicationTopologyPage.ts exists with 6+ methods
- [ ] frontend/e2e/pages/DataClassificationPage.ts exists with 8+ methods
- [ ] frontend/e2e/pages/HostInventoryPage.ts exists with 9+ methods
- [ ] frontend/e2e/tests/12-replication-topology.spec.ts exists with 6+ tests
- [ ] frontend/e2e/tests/13-data-classification.spec.ts exists with 8+ tests
- [ ] frontend/e2e/tests/14-host-monitoring.spec.ts exists with 9+ tests
- [ ] All E2E tests pass: `cd frontend && npx playwright test`
- [ ] Tests login before each test case
- [ ] Page objects follow existing pattern from LoginPage.ts
</success_criteria>

<output>
After completion, create `.planning/phases/14-testing-quality/14-04-SUMMARY.md`
</output>